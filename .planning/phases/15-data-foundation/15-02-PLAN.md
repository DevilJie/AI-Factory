---
phase: 15-data-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - ai-factory-backend/src/main/java/com/aifactory/entity/NovelChapterPlan.java
  - ai-factory-backend/src/main/java/com/aifactory/dto/ChapterPlanDto.java
  - ai-factory-backend/src/main/java/com/aifactory/dto/ChapterPlanUpdateRequest.java
  - ai-factory-backend/src/main/java/com/aifactory/service/ChapterService.java
  - ai-factory-frontend/src/types/project.ts
  - ai-factory-frontend/src/api/chapter.ts
  - ai-factory-frontend/src/views/Project/Detail/creation/ChapterPlanDrawer.vue
  - ai-factory-frontend/src/views/Project/Detail/creation/VolumeTree.vue
  - ai-factory-frontend/src/views/Project/Detail/creation/CreationCenter.vue
  - sql/init.sql
autonomous: true
requirements:
  - DATA-02

must_haves:
  truths:
    - "novel_chapter_plan 的 foreshadowingSetup/foreshadowingPayoff 字段从数据库列、实体类、DTO、前端类型中完全删除"
    - "前端编译通过，无 foreshadowingSetup/foreshadowingPayoff 引用残留"
    - "后端编译通过，ChapterService 中无 foreshadowingSetup/foreshadowingPayoff 引用残留"
  artifacts:
    - path: "ai-factory-backend/src/main/java/com/aifactory/entity/NovelChapterPlan.java"
      provides: "Entity without foreshadowingSetup/foreshadowingPayoff"
      contains: "plannedCharacters"
      not_contains: "foreshadowingSetup"
    - path: "ai-factory-frontend/src/types/project.ts"
      provides: "TypeScript types without foreshadowingSetup/foreshadowingPayoff"
      not_contains: "foreshadowingSetup"
    - path: "sql/init.sql"
      provides: "novel_chapter_plan CREATE TABLE without foreshadowing_setup/payoff columns"
      not_contains: "foreshadowing_setup"
  key_links:
    - from: "ChapterService.java"
      to: "ChapterPlanDto / ChapterPlanUpdateRequest"
      via: "field mapping"
      pattern: "foreshadowingSetup|foreshadowingPayoff"
      must_be_absent: true
    - from: "ChapterPlanDrawer.vue"
      to: "project.ts ChapterPlan type"
      via: "v-model and field access"
      pattern: "foreshadowingSetup|foreshadowingPayoff"
      must_be_absent: true
---

<objective>
Completely delete foreshadowingSetup and foreshadowingPayoff from all layers: Java entity, DTOs, service, frontend types, frontend components, and SQL schema. Per D-09 and D-10, this is a full deletion with no soft deprecation.

Purpose: These text fields are redundant with the structured novel_foreshadowing table. Removing them eliminates data duplication and simplifies the codebase.
Output: Clean codebase with zero references to foreshadowingSetup/foreshadowingPayoff.
</objective>

<execution_context>
@D:/work/ai/AI-Factory/.claude/get-shit-done/workflows/execute-plan.md
@D:/work/ai/AI-Factory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-data-foundation/15-CONTEXT.md
@.planning/phases/15-data-foundation/15-RESEARCH.md

<!-- Depends on 15-01 only because both modify sql/init.sql. 15-01 adds columns, 15-02 removes columns from different table section. -->

<interfaces>
<!-- Files that contain foreshadowingSetup/foreshadowingPayoff to be deleted, with exact line references -->

From ai-factory-backend/src/main/java/com/aifactory/entity/NovelChapterPlan.java (lines 95-103):
```java
// DELETE these 2 fields (lines 95-103):
/**
 * 埋伏笔（JSON格式）
 */
private String foreshadowingSetup;

/**
 * 填伏笔（JSON格式）
 */
private String foreshadowingPayoff;
```

From ai-factory-backend/src/main/java/com/aifactory/dto/ChapterPlanDto.java (lines 57-61):
```java
// DELETE these 2 fields:
@Schema(description = "待埋设的伏笔，用分号分隔", example = "主角腰间的玉佩;老者的神秘身份")
private String foreshadowingSetup;

@Schema(description = "待回收的伏笔，用分号分隔", example = "第一章提到的失踪父亲线索")
private String foreshadowingPayoff;
```

From ai-factory-backend/src/main/java/com/aifactory/dto/ChapterPlanUpdateRequest.java (lines 44-48):
```java
// DELETE these 2 fields:
@Schema(description = "埋伏笔")
private String foreshadowingSetup;

@Schema(description = "填伏笔")
private String foreshadowingPayoff;
```

From ai-factory-backend/src/main/java/com/aifactory/service/ChapterService.java -- 4 locations:
- Lines 218-219: dto.setForeshadowingSetup / dto.setForeshadowingPayoff (in plan-to-dto mapping)
- Lines 322-326: request.getForeshadowingSetup / request.getForeshadowingPayoff (in update handler)
- Lines 570-571: dto.setForeshadowingSetup / dto.setForeshadowingPayoff (in another plan-to-dto mapping)
- Lines 1290-1294: plan.getForeshadowingSetup / plan.getForeshadowingPayoff (in prompt building)

From ai-factory-frontend/src/types/project.ts:
- Lines 132-133: foreshadowingSetup and foreshadowingPayoff in Chapter interface
- Lines 156-157: foreshadowingSetup and foreshadowingPayoff in ChapterPlan interface

From ai-factory-frontend/src/api/chapter.ts:
- Lines 41-42: foreshadowingSetup and foreshadowingPayoff in ChapterPlanUpdateRequest interface

From ai-factory-frontend/src/views/Project/Detail/creation/ChapterPlanDrawer.vue:
- Lines 30-31: form field type declarations
- Lines 45-46: form default values
- Lines 250-251: plan-to-form mapping
- Lines 279-280: chapter-to-form mapping
- Lines 308-309: form-to-save payload
- Lines 494: v-model="form.foreshadowingSetup" textarea
- Lines 510: v-model="form.foreshadowingPayoff" textarea

From ai-factory-frontend/src/views/Project/Detail/creation/VolumeTree.vue:
- Lines 159-160: chapter plan construction

From ai-factory-frontend/src/views/Project/Detail/creation/CreationCenter.vue:
- Lines 115-116: chapter plan construction

From sql/init.sql (novel_chapter_plan CREATE TABLE):
- Lines 174-175: foreshadowing_setup and foreshadowing_payoff columns
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete foreshadowingSetup/foreshadowingPayoff from backend (entity, DTOs, service, SQL)</name>
  <files>
    ai-factory-backend/src/main/java/com/aifactory/entity/NovelChapterPlan.java
    ai-factory-backend/src/main/java/com/aifactory/dto/ChapterPlanDto.java
    ai-factory-backend/src/main/java/com/aifactory/dto/ChapterPlanUpdateRequest.java
    ai-factory-backend/src/main/java/com/aifactory/service/ChapterService.java
    sql/init.sql
  </files>
  <read_first>
    ai-factory-backend/src/main/java/com/aifactory/entity/NovelChapterPlan.java
    ai-factory-backend/src/main/java/com/aifactory/dto/ChapterPlanDto.java
    ai-factory-backend/src/main/java/com/aifactory/dto/ChapterPlanUpdateRequest.java
    ai-factory-backend/src/main/java/com/aifactory/service/ChapterService.java
    sql/init.sql (lines 160-184 for novel_chapter_plan CREATE TABLE)
  </read_first>
  <action>
    Per D-09, D-10: Complete deletion of foreshadowingSetup/foreshadowingPayoff from all backend layers. No soft deprecation.

    **Delete in order (frontend-first pattern, but since frontend is Task 2, do backend first here -- they are in same plan):**

    1. **NovelChapterPlan.java** -- Delete lines 95-103 (the foreshadowingSetup field + javadoc and foreshadowingPayoff field + javadoc). Remove exactly these lines:
       ```java
       /**
        * 埋伏笔（JSON格式）
        */
       private String foreshadowingSetup;

       /**
        * 填伏笔（JSON格式）
        */
       private String foreshadowingPayoff;
       ```

    2. **ChapterPlanDto.java** -- Delete lines 57-61 (both @Schema + field declarations):
       ```java
       @Schema(description = "待埋设的伏笔，用分号分隔", example = "主角腰间的玉佩;老者的神秘身份")
       private String foreshadowingSetup;

       @Schema(description = "待回收的伏笔，用分号分隔", example = "第一章提到的失踪父亲线索")
       private String foreshadowingPayoff;
       ```

    3. **ChapterPlanUpdateRequest.java** -- Delete lines 44-48 (both @Schema + field declarations):
       ```java
       @Schema(description = "埋伏笔")
       private String foreshadowingSetup;

       @Schema(description = "填伏笔")
       private String foreshadowingPayoff;
       ```

    4. **ChapterService.java** -- Delete at ALL 4 locations:
       - **Lines ~218-219**: Delete both setter calls:
         ```java
         dto.setForeshadowingSetup(plan.getForeshadowingSetup());
         dto.setForeshadowingPayoff(plan.getForeshadowingPayoff());
         ```
       - **Lines ~322-326**: Delete the entire if-block for foreshadowingSetup and the entire if-block for foreshadowingPayoff:
         ```java
         if (request.getForeshadowingSetup() != null) {
             plan.setForeshadowingSetup(request.getForeshadowingSetup());
         }
         if (request.getForeshadowingPayoff() != null) {
             plan.setForeshadowingPayoff(request.getForeshadowingPayoff());
         }
         ```
       - **Lines ~570-571**: Delete both setter calls:
         ```java
         dto.setForeshadowingSetup(chapterPlan.getForeshadowingSetup());
         dto.setForeshadowingPayoff(chapterPlan.getForeshadowingPayoff());
         ```
       - **Lines ~1290-1294**: Delete the two if-blocks that append foreshadowing to prompt:
         ```java
         if (plan.getForeshadowingSetup() != null) {
             prompt.append("- 埋伏笔：").append(plan.getForeshadowingSetup()).append("\n");
         }
         if (plan.getForeshadowingPayoff() != null) {
             prompt.append("- 填伏笔：").append(plan.getForeshadowingPayoff()).append("\n");
         }
         ```

    5. **sql/init.sql** -- In the novel_chapter_plan CREATE TABLE (around lines 174-175), delete these two lines:
       ```sql
       `foreshadowing_setup` text CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL COMMENT '埋伏笔',
       `foreshadowing_payoff` text CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL COMMENT '填伏笔',
       ```
       Also add to the migration file `sql/foreshadowing_volume_migration.sql` (created in 15-01):
       ```sql
       -- Remove redundant foreshadowing text fields from novel_chapter_plan
       ALTER TABLE novel_chapter_plan
         DROP COLUMN foreshadowing_setup,
         DROP COLUMN foreshadowing_payoff;
       ```
  </action>
  <verify>
    <automated>cd ai-factory-backend && grep -r "foreshadowingSetup\|foreshadowingPayoff\|foreshadowing_setup\|foreshadowing_payoff" src/ | grep -v ".class" | wc -l</automated>
  </verify>
  <done>
    grep for "foreshadowingSetup|foreshadowingPayoff" in ai-factory-backend/src/ returns 0 results
    grep for "foreshadowing_setup|foreshadowing_payoff" in sql/init.sql novel_chapter_plan section returns 0 results
    NovelChapterPlan.java has no foreshadowingSetup or foreshadowingPayoff fields
    ChapterPlanDto.java has no foreshadowingSetup or foreshadowingPayoff fields
    ChapterPlanUpdateRequest.java has no foreshadowingSetup or foreshadowingPayoff fields
    ChapterService.java has no foreshadowingSetup or foreshadowingPayoff references
    sql/init.sql novel_chapter_plan CREATE TABLE has no foreshadowing_setup or foreshadowing_payoff columns
    sql/foreshadowing_volume_migration.sql contains DROP COLUMN statements
    mvn compile succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete foreshadowingSetup/foreshadowingPayoff from frontend (types, API, components)</name>
  <files>
    ai-factory-frontend/src/types/project.ts
    ai-factory-frontend/src/api/chapter.ts
    ai-factory-frontend/src/views/Project/Detail/creation/ChapterPlanDrawer.vue
    ai-factory-frontend/src/views/Project/Detail/creation/VolumeTree.vue
    ai-factory-frontend/src/views/Project/Detail/creation/CreationCenter.vue
  </files>
  <read_first>
    ai-factory-frontend/src/types/project.ts (lines 120-170)
    ai-factory-frontend/src/api/chapter.ts (lines 28-50)
    ai-factory-frontend/src/views/Project/Detail/creation/ChapterPlanDrawer.vue (full file)
    ai-factory-frontend/src/views/Project/Detail/creation/VolumeTree.vue (lines 150-170)
    ai-factory-frontend/src/views/Project/Detail/creation/CreationCenter.vue (lines 105-125)
  </read_first>
  <action>
    Per D-09: Complete deletion of foreshadowingSetup/foreshadowingPayoff from all frontend files.

    1. **ai-factory-frontend/src/types/project.ts**:
       - In `Chapter` interface (~line 132-133): Delete these two lines:
         ```typescript
         foreshadowingSetup?: string
         foreshadowingPayoff?: string
         ```
       - In `ChapterPlan` interface (~line 156-157): Delete these two lines:
         ```typescript
         foreshadowingSetup?: string
         foreshadowingPayoff?: string
         ```

    2. **ai-factory-frontend/src/api/chapter.ts**:
       - In `ChapterPlanUpdateRequest` interface (~lines 41-42): Delete these two lines:
         ```typescript
         foreshadowingSetup?: string
         foreshadowingPayoff?: string
         ```

    3. **ai-factory-frontend/src/views/Project/Detail/creation/ChapterPlanDrawer.vue**:
       Read the FULL file first to understand the exact structure. Then delete all foreshadowingSetup/foreshadowingPayoff references:
       - In form type definition (~lines 30-31): Remove `foreshadowingSetup: string` and `foreshadowingPayoff: string`
       - In form default values (~lines 45-46): Remove `foreshadowingSetup: ''` and `foreshadowingPayoff: ''`
       - In plan-to-form mapping (~lines 250-251): Remove `foreshadowingSetup: plan.foreshadowingSetup || ''` and `foreshadowingPayoff: plan.foreshadowingPayoff || ''`
       - In chapter-to-form mapping (~lines 279-280): Remove `foreshadowingSetup: chapter.foreshadowingSetup || ''` and `foreshadowingPayoff: chapter.foreshadowingPayoff || ''`
       - In save payload (~lines 308-309): Remove `foreshadowingSetup: form.value.foreshadowingSetup` and `foreshadowingPayoff: form.value.foreshadowingPayoff`
       - In template (~lines 494 and 510): Remove the two textarea elements with `v-model="form.foreshadowingSetup"` and `v-model="form.foreshadowingPayoff"`. Also remove their parent container divs/labels if the only content is the foreshadowing textarea.

    4. **ai-factory-frontend/src/views/Project/Detail/creation/VolumeTree.vue**:
       - ~Lines 159-160: Remove these two lines from the chapter plan object construction:
         ```typescript
         foreshadowingSetup: (chapter as any).foreshadowingSetup || chapter.foreshadowingSetup,
         foreshadowingPayoff: (chapter as any).foreshadowingPayoff || chapter.foreshadowingPayoff,
         ```

    5. **ai-factory-frontend/src/views/Project/Detail/creation/CreationCenter.vue**:
       - ~Lines 115-116: Remove these two lines from the chapter plan object construction:
         ```typescript
         foreshadowingSetup: (chapter as any).foreshadowingSetup || chapter.foreshadowingSetup,
         foreshadowingPayoff: (chapter as any).foreshadowingPayoff || chapter.foreshadowingPayoff,
         ```
  </action>
  <verify>
    <automated>cd ai-factory-frontend && grep -r "foreshadowingSetup\|foreshadowingPayoff" src/ | wc -l</automated>
  </verify>
  <done>
    grep for "foreshadowingSetup|foreshadowingPayoff" in ai-factory-frontend/src/ returns 0 results
    project.ts Chapter interface has no foreshadowingSetup or foreshadowingPayoff
    project.ts ChapterPlan interface has no foreshadowingSetup or foreshadowingPayoff
    chapter.ts ChapterPlanUpdateRequest has no foreshadowingSetup or foreshadowingPayoff
    ChapterPlanDrawer.vue has no foreshadowingSetup or foreshadowingPayoff references in script or template
    VolumeTree.vue has no foreshadowingSetup or foreshadowingPayoff references
    CreationCenter.vue has no foreshadowingSetup or foreshadowingPayoff references
    TypeScript compilation passes (npx tsc --noEmit)
  </done>
</task>

</tasks>

<verification>
1. `cd ai-factory-backend && grep -r "foreshadowingSetup\|foreshadowingPayoff" src/ | wc -l` returns 0
2. `cd ai-factory-frontend && grep -r "foreshadowingSetup\|foreshadowingPayoff" src/ | wc -l` returns 0
3. `cd ai-factory-backend && mvn compile -q` -- backend compiles
4. `cd ai-factory-frontend && npx tsc --noEmit` -- frontend TypeScript check passes
5. `grep "foreshadowing_setup\|foreshadowing_payoff" sql/init.sql` returns only the DROP COLUMN in migration file (not in CREATE TABLE)
</verification>

<success_criteria>
- Zero references to foreshadowingSetup/foreshadowingPayoff in entire codebase (backend + frontend)
- sql/init.sql novel_chapter_plan CREATE TABLE has no foreshadowing_setup or foreshadowing_payoff columns
- sql/foreshadowing_volume_migration.sql contains ALTER TABLE DROP COLUMN statements
- Backend compiles without errors
- Frontend TypeScript check passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-foundation/15-02-SUMMARY.md`
</output>
