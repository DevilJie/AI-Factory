---
mode: quick
quick_id: "260405-0l2"
description: "适配力量体系模板XML结构变化：删除lls/steps包裹节点"
date: 2026-04-05
---

# Quick Task Plan: 适配力量体系模板XML结构变化

## Context

用户在数据库中修改了力量体系提示词模板（`llm_power_system_create`），将 `<lls>` 包裹节点删除（`<ll>` 直接在 `<ss>` 下），将 `<steps>` 包裹节点删除（`<step>` 直接在 `<ll>` 下）。后端 `WorldviewXmlParser` 的解析逻辑需要同步适配。

## Tasks

### Task 1: 修改 parseSingleSystem() 支持 `<ll>` 直接子节点

- **files**: `ai-factory-backend/src/main/java/com/aifactory/common/WorldviewXmlParser.java`
- **action**: 在 `parseSingleSystem()` 方法的 switch 中，保留 `case "lls"`（向后兼容），新增 `case "ll"` 直接解析单个 `<ll>` 节点
- **verify**: 编译通过
- **done**: `<ll>` 作为 `<ss>` 直接子节点能被正确解析为 NovelPowerSystemLevel

### Task 2: 修改 parseSingleLevel() 支持 `<step>` 直接子节点

- **files**: `ai-factory-backend/src/main/java/com/aifactory/common/WorldviewXmlParser.java`
- **action**: 在 `parseSingleLevel()` 方法的 switch 中，保留 `case "steps"`（向后兼容），新增 `case "step"` 直接解析单个 `<step>` 节点
- **verify**: 编译通过
- **done**: `<step>` 作为 `<ll>` 直接子节点能被正确解析为 NovelPowerSystemLevelStep

### Task 3: 更新单元测试覆盖新模板格式

- **files**: `ai-factory-backend/src/test/java/com/aifactory/common/WorldviewXmlParserTest.java`
- **action**: 新增测试用 `parsePowerSystemXml` 方法测试：无 `<lls>` 包裹、无 `<steps>` 包裹的新格式 XML
- **verify**: 测试通过
- **done**: 新格式测试全部通过，旧格式测试仍然通过
