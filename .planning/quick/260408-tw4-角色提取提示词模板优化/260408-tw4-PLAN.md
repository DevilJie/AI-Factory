---
phase: quick
plan: 260408-tw4
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-factory-backend/src/main/java/com/aifactory/dto/ChapterCharacterExtractXmlDto.java
  - ai-factory-backend/src/main/java/com/aifactory/service/ChapterCharacterExtractService.java
  - sql/init.sql
autonomous: true

must_haves:
  truths:
    - "LLM输出的修炼体系XML包含独立的<RL>大境界和<SL>小境界标签，不再混合为<LV>"
    - "提示词模板明确要求AI输出的境界名称必须与力量体系表严格一致"
    - "提示词模板包含力量体系上下文数据（大境界及其对应小境界列表）"
    - "后端XML解析正确提取<RL>和<SL>为独立字段"
    - "后端角色-力量体系关联匹配使用拆分后的字段，精确匹配大境界和小境界"
  artifacts:
    - path: "ai-factory-backend/src/main/java/com/aifactory/dto/ChapterCharacterExtractXmlDto.java"
      provides: "CultivationSystemDto with realmLevel and subLevel fields replacing currentLevel"
      contains: "realmLevel"
    - path: "sql/init.sql"
      provides: "Updated template_version id=21 with new V tag format"
      contains: "<RL>"
  key_links:
    - from: "ChapterCharacterExtractXmlDto.CultivationSystemDto"
      to: "ChapterCharacterExtractService.resolveAndSavePowerSystemAssociations"
      via: "realmLevel + subLevel fields"
      pattern: "getRealmLevel|getSubLevel"
    - from: "ChapterCharacterExtractService.buildPowerSystemConstraint"
      to: "PowerSystemService.buildPowerSystemConstraint"
      via: "power system constraint text injection into prompt"
      pattern: "buildPowerSystemConstraint"
---

<objective>
Split the combined `<LV>` tag in character extraction XML into separate `<RL>` (大境界) and `<SL>` (小境界) tags, update the prompt template to require strict name matching against power system tables, and update backend parsing to use the split fields for accurate matching.

Purpose: The current `<LV>` combines major/minor realm as "大境界（小境界）", requiring fragile fuzzy matching. Split tags enable exact matching against `novel_power_system_level.level_name` and `novel_power_system_level_step.level_name`, improving match accuracy.

Output: Updated DTO, service, SQL migration for template_version table.
</objective>

<execution_context>
@D:/work/ai/AI-Factory/.claude/get-shit-done/workflows/execute-plan.md
@D:/work/ai/AI-Factory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
<!-- Current CultivationSystemDto fields (to be replaced) -->
From ChapterCharacterExtractXmlDto.java:
```java
public static class CultivationSystemDto {
    @JacksonXmlProperty(localName = "SYS")
    private String systemName;        //修炼体系名称

    @JacksonXmlProperty(localName = "LV")
    private String currentLevel;      //当前境界等级 (combined "大境界（小境界）")

    @JacksonXmlProperty(localName = "CH")
    private String levelChange;       //本章境界变化
}
```

<!-- Power system table structure -->
```java
// novel_power_system_level: levelName = 大境界 (e.g., "醒血境")
// novel_power_system_level_step: levelName = 小境界 (e.g., "感血")
// NovelCharacterPowerSystem: currentRealmId -> level.id, currentSubRealmId -> step.id
```

<!-- Active template: id=19, template_id=15, version=2 -->
<!-- Template uses {chapterInfo} and {existingRoleDistribution} variables -->
<!-- ChapterCharacterExtractService.buildCharacterExtractPrompt() builds chapterInfo -->
<!-- which includes buildPowerSystemConstraint() output inline -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DTO and service to support split RL/SL fields</name>
  <files>
    ai-factory-backend/src/main/java/com/aifactory/dto/ChapterCharacterExtractXmlDto.java
    ai-factory-backend/src/main/java/com/aifactory/service/ChapterCharacterExtractService.java
  </files>
  <action>
**1. Update ChapterCharacterExtractXmlDto.CultivationSystemDto:**

Replace the single `currentLevel` field (mapped to `<LV>`) with two fields:
- `realmLevel` mapped to `<RL>` (大境界, e.g., "醒血境")
- `subLevel` mapped to `<SL>` (小境界, e.g., "感血")

Keep the old `currentLevel` / `<LV>` field annotated with `@JacksonXmlProperty(localName = "LV")` for backward compatibility (old LLM responses may still use it). Add a helper method `getEffectiveRealmLevel()` that returns `realmLevel` if non-null, otherwise falls back to extracting the major realm part from `currentLevel` (the part before "（"). Similarly `getEffectiveSubLevel()` returns `subLevel` if non-null, otherwise extracts the minor realm from `currentLevel` (the part inside "（）").

Update all Javadoc comments to reflect the new XML format:
```
<V>
  <SYS>古神道</SYS>
  <RL>醒血境</RL>
  <SL>感血</SL>
  <CH>描述...</CH>
</V>
```

**2. Update ChapterCharacterExtractService:**

2a. In `parseCultivationLevelJson()`: Update the JSON map to include `realmLevel` and `subLevel` keys instead of just `currentLevel`. Also include `currentLevel` as fallback for backward compat. The map should contain:
- "systemName" -> system.getSystemName()
- "realmLevel" -> effectiveRealmLevel
- "subLevel" -> effectiveSubLevel
- "levelChange" -> system.getLevelChange()

2b. In `resolveAndSavePowerSystemAssociations()`: Replace the currentLevel-based matching logic. Instead of fuzzy-matching `csDto.getCurrentLevel()` against both level names and step names:
- Use `csDto.getEffectiveRealmLevel()` for exact match against `NovelPowerSystemLevel.levelName` (still with fuzzy fallback)
- Use `csDto.getEffectiveSubLevel()` for exact match against `NovelPowerSystemLevelStep.levelName` (still with fuzzy fallback)
- The sub-level matching now only happens within the already-matched realm level (same as current, but cleaner since input is separate)

This is a targeted refactor — the matching strategy (exact then fuzzy) stays the same, just the source fields change.
  </action>
  <verify>
    <automated>cd D:/work/ai/AI-Factory/ai-factory-backend && mvn compile -q 2>&1 | tail -5</automated>
  </verify>
  <done>CultivationSystemDto has realmLevel/subLevel fields with backward compat for currentLevel. resolveAndSavePowerSystemAssociations uses the new fields for cleaner matching. parseCultivationLevelJson stores split fields in JSON. Code compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create new prompt template version with split RL/SL format and strict power system constraints</name>
  <files>sql/init.sql</files>
  <action>
Add a new `ai_prompt_template_version` record (id=21) for template_id=15 (llm_chapter_character_extract), version_number=3. This replaces the current v2 (id=19).

The new template content must:
1. Update the XML output example to show `<RL>` and `<SL>` instead of `<LV>`:
```xml
<V>
  <SYS>修炼体系名称</SYS>
  <RL>大境界名称</RL>
  <SL>小境界名称</SL>
  <CH>本章境界变化</CH>
</V>
```

2. Add explicit instruction that `<SYS>`, `<RL>`, `<SL>` values MUST exactly match the names listed in the修炼体系设定 section. Example wording:
```
## 修炼体系输出约束（极其重要）
- <SYS>必须是上述修炼体系的名称（如"古神道"）
- <RL>必须是大境界名称（如"醒血境"），必须与等级划分中的大境界完全一致
- <SL>必须是小境界名称（如"感血"），必须与括号中的小境界完全一致
- 如果不确定具体小境界，<SL>可以省略，但<RL>必须准确
- 禁止编造不存在的修炼体系、大境界或小境界名称
```

3. Keep all existing template content (role type definitions, FC faction tags, existingRoleDistribution variable, chapterInfo variable, etc.) from the v2 template (id=19). Only modify the V-tag format section and add the new constraint section.

Also add SQL to deactivate v2 and activate v3:
```sql
UPDATE `ai_prompt_template_version` SET `is_active` = 0 WHERE `id` = 19 AND `template_id` = 15;
UPDATE `ai_prompt_template` SET `current_version_id` = 21 WHERE `id` = 15;
```

Note: The `buildPowerSystemConstraint()` method in PowerSystemServiceImpl already generates text listing each system with level names and step names in format "大境界（小境界1/小境界2）". This existing output is sufficient context for the LLM to produce correct RL/SL values — no changes needed to PowerSystemServiceImpl.

The `variable_definitions` JSON should add `realmLevel` and `subLevel` descriptions but otherwise match v2.
  </action>
  <verify>
    <automated>cd D:/work/ai/AI-Factory && grep -c "id.*21.*template_id.*15" sql/init.sql</automated>
  </verify>
  <done>New template_version id=21 exists in init.sql with RL/SL format and strict power system constraints. v2 (id=19) deactivated. Template table current_version_id updated to 21.</done>
</task>

</tasks>

<verification>
1. `cd D:/work/ai/AI-Factory/ai-factory-backend && mvn compile -q` — compiles cleanly
2. `cd D:/work/ai/AI-Factory && grep -c "<RL>" sql/init.sql` — at least 1 match (new template)
3. `cd D:/work/ai/AI-Factory && grep -c "realmLevel" ai-factory-backend/src/main/java/com/aifactory/dto/ChapterCharacterExtractXmlDto.java` — at least 1 match
</verification>

<success_criteria>
- CultivationSystemDto has `realmLevel` (mapped to `<RL>`) and `subLevel` (mapped to `<SL>`) with backward compat for `<LV>`
- resolveAndSavePowerSystemAssociations uses realmLevel for level matching and subLevel for step matching
- New template version in init.sql instructs LLM to output RL/SL separately with strict name matching
- Existing functionality (FC tags, role types, faction list) unchanged
- Code compiles without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260408-tw4-角色提取提示词模板优化/260408-tw4-SUMMARY.md`
</output>
