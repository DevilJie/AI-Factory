---
phase: 01-数据基础
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionRegion.java
  - ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionCharacter.java
  - ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionRelation.java
  - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionRegionMapper.java
  - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionCharacterMapper.java
  - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionRelationMapper.java
  - ai-factory-backend/src/main/java/com/aifactory/entity/NovelWorldview.java
autonomous: true
requirements:
  - DB-03
  - DB-04
  - DB-05
  - DB-06

must_haves:
  truths:
    - "NovelFactionRegion.java 实体映射 novel_faction_region 表，只有 id、factionId、regionId 三个持久化字段"
    - "NovelFactionCharacter.java 实体映射 novel_faction_character 表，包含 id、factionId、characterId、role 字段"
    - "NovelFactionRelation.java 实体映射 novel_faction_relation 表，包含 id、factionId、targetFactionId、relationType、description 字段"
    - "3 个关联 Mapper 均继承 BaseMapper 并标注 @Mapper"
    - "NovelWorldview.java 的 forces 字段标注 @TableField(exist = false)"
    - "全部 7 个新建/修改文件编译通过"
  artifacts:
    - path: "ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionRegion.java"
      provides: "势力-地区关联实体"
      contains: "@TableName(\"novel_faction_region\")"
    - path: "ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionCharacter.java"
      provides: "势力-人物关联实体"
      contains: "@TableName(\"novel_faction_character\")"
    - path: "ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionRelation.java"
      provides: "势力-势力关系实体"
      contains: "@TableName(\"novel_faction_relation\")"
    - path: "ai-factory-backend/src/main/java/com/aifactory/entity/NovelWorldview.java"
      provides: "世界观实体（forces 字段改为 transient）"
      contains: "@TableField(exist = false)"
  key_links:
    - from: "NovelFactionRegion.java"
      to: "novel_faction_region table"
      via: "@TableName annotation"
      pattern: "@TableName\\(\"novel_faction_region\"\\)"
    - from: "NovelFactionCharacter.java"
      to: "novel_faction_character table"
      via: "@TableName annotation"
      pattern: "@TableName\\(\"novel_faction_character\"\\)"
    - from: "NovelFactionRelation.java"
      to: "novel_faction_relation table"
      via: "@TableName annotation"
      pattern: "@TableName\\(\"novel_faction_relation\"\\)"
    - from: "NovelWorldview.java forces field"
      to: "novel_worldview.forces column"
      via: "@TableField(exist = false) — dissociates from DB column"
      pattern: "@TableField\\(exist = false\\)"
---

<objective>
创建 3 个关联表实体和 Mapper，修改 NovelWorldview.java 的 forces 字段为 transient。

Purpose: 关联表实体是势力-地区、势力-人物、势力-势力关系的数据层基础。将 forces 字段标记为 transient 是势力数据源从纯文本列切换到结构化表的关键步骤（D-11）。

Output: 3 个关联实体类、3 个 Mapper 接口、NovelWorldview.java 修改
</objective>

<execution_context>
@D:/work/ai/AI-Factory/.claude/get-shit-done/workflows/execute-plan.md
@D:/work/ai/AI-Factory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-数据基础/01-CONTEXT.md
@.planning/phases/01-数据基础/01-RESEARCH.md
@.planning/phases/01-数据基础/01-VALIDATION.md

<interfaces>
<!-- Reference implementations the executor must follow -->

From ai-factory-backend/src/main/java/com/aifactory/entity/NovelWorldviewPowerSystem.java:
```java
@Data
@TableName("novel_worldview_power_system")
public class NovelWorldviewPowerSystem {
    @TableId(type = IdType.AUTO)
    private Long id;
    private Long worldviewId;
    private Long powerSystemId;
}
```

From ai-factory-backend/src/main/java/com/aifactory/entity/NovelWorldview.java (current state):
```java
@Data
@TableName("novel_worldview")
public class NovelWorldview {
    @TableId(type = IdType.AUTO)
    private Long id;
    private Long userId;
    private Long projectId;
    private Long outlineId;
    private String worldType;
    private String worldBackground;
    /** 势力分布 */
    private String forces;            // <-- THIS FIELD NEEDS @TableField(exist = false)
    /** 时间线设定 */
    private String timeline;
    /** 世界规则 */
    private String rules;
    /** 主角ID列表 (JSON) */
    private String protagonistIds;
    private LocalDateTime createTime;
    private LocalDateTime updateTime;
    @TableField(exist = false)
    private String geography;         // <-- EXISTING PRECEDENT for transient field
}
```

From ai-factory-backend/src/main/java/com/aifactory/mapper/NovelContinentRegionMapper.java:
```java
@Mapper
public interface NovelContinentRegionMapper extends BaseMapper<NovelContinentRegion> {
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 3 个关联表实体和 3 个 Mapper 接口</name>
  <files>
    - ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionRegion.java
    - ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionCharacter.java
    - ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionRelation.java
    - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionRegionMapper.java
    - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionCharacterMapper.java
    - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionRelationMapper.java
  </files>
  <read_first>
    - ai-factory-backend/src/main/java/com/aifactory/entity/NovelWorldviewPowerSystem.java (关联表实体模式参考)
    - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelContinentRegionMapper.java (Mapper 模式参考)
  </read_first>
  <action>
创建 6 个文件，严格复用 NovelWorldviewPowerSystem.java 和 NovelContinentRegionMapper.java 的模式。

**NovelFactionRegion.java** (参照 NovelWorldviewPowerSystem.java，最简关联表)：
```java
package com.aifactory.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;

@Data
@TableName("novel_faction_region")
public class NovelFactionRegion {

    @TableId(type = IdType.AUTO)
    private Long id;

    private Long factionId;

    private Long regionId;
}
```

**NovelFactionCharacter.java** (参照 NovelWorldviewPowerSystem.java，增加 role 字段 per D-07)：
```java
package com.aifactory.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;

@Data
@TableName("novel_faction_character")
public class NovelFactionCharacter {

    @TableId(type = IdType.AUTO)
    private Long id;

    private Long factionId;

    private Long characterId;

    /** 职位（如掌门、长老、弟子，自由文本 per D-07） */
    private String role;
}
```

**NovelFactionRelation.java** (参照 NovelWorldviewPowerSystem.java，扩展为双向关系 per D-06)：
```java
package com.aifactory.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;

@Data
@TableName("novel_faction_relation")
public class NovelFactionRelation {

    @TableId(type = IdType.AUTO)
    private Long id;

    private Long factionId;

    private Long targetFactionId;

    /** 关系类型（ally/hostile/neutral，双向存储 per D-06） */
    private String relationType;

    /** 关系描述 */
    private String description;
}
```

**3 个 Mapper 接口** (完全复用 NovelContinentRegionMapper.java 模式)：

NovelFactionRegionMapper.java：
```java
package com.aifactory.mapper;

import com.aifactory.entity.NovelFactionRegion;
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface NovelFactionRegionMapper extends BaseMapper<NovelFactionRegion> {
}
```

NovelFactionCharacterMapper.java：
```java
package com.aifactory.mapper;

import com.aifactory.entity.NovelFactionCharacter;
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface NovelFactionCharacterMapper extends BaseMapper<NovelFactionCharacter> {
}
```

NovelFactionRelationMapper.java：
```java
package com.aifactory.mapper;

import com.aifactory.entity.NovelFactionRelation;
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface NovelFactionRelationMapper extends BaseMapper<NovelFactionRelation> {
}
```

关键约束：
- NovelFactionRegion 只有 3 个字段（id、factionId、regionId），不加 description（D-09）
- NovelFactionCharacter 的 role 字段使用 String 类型（D-07 自由文本）
- NovelFactionRelation 的 relationType 使用 String 存储 ally/hostile/neutral 英文编码
- 所有 Mapper 纯继承 BaseMapper，不加自定义方法
  </action>
  <verify>
    <automated>cd D:/work/ai/AI-Factory/ai-factory-backend && mvn compile -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - NovelFactionRegion.java 包含 @TableName("novel_faction_region") 和字段 id(Long), factionId(Long), regionId(Long)
    - NovelFactionCharacter.java 包含 @TableName("novel_faction_character") 和字段 id(Long), factionId(Long), characterId(Long), role(String)
    - NovelFactionRelation.java 包含 @TableName("novel_faction_relation") 和字段 id(Long), factionId(Long), targetFactionId(Long), relationType(String), description(String)
    - 3 个 Mapper 文件均包含 @Mapper 注解
    - NovelFactionRegionMapper 继承 BaseMapper&lt;NovelFactionRegion&gt;
    - NovelFactionCharacterMapper 继承 BaseMapper&lt;NovelFactionCharacter&gt;
    - NovelFactionRelationMapper 继承 BaseMapper&lt;NovelFactionRelation&gt;
    - mvn compile 编译通过（返回码 0）
  </acceptance_criteria>
  <done>3 个关联表实体和 3 个 Mapper 接口已创建，编译通过，字段与 DDL 一一对应</done>
</task>

<task type="auto">
  <name>Task 2: 修改 NovelWorldview.java 的 forces 字段为 transient</name>
  <files>
    - ai-factory-backend/src/main/java/com/aifactory/entity/NovelWorldview.java
  </files>
  <read_first>
    - ai-factory-backend/src/main/java/com/aifactory/entity/NovelWorldview.java (当前完整内容，定位 forces 字段和 geography 字段先例)
  </read_first>
  <action>
修改 ai-factory-backend/src/main/java/com/aifactory/entity/NovelWorldview.java 中的 forces 字段。

**当前代码（第 23-24 行）：**
```java
    /** 势力分布 */
    private String forces;
```

**改为：**
```java
    /**
     * 势力分布（非数据库字段，由 FactionService 从 novel_faction 表构建）
     */
    @TableField(exist = false)
    private String forces;
```

关键点：
- 添加 @TableField(exist = false) 注解，与同文件中的 geography 字段（第 38-39 行）保持完全一致的标注方式
- 更新注释说明数据来源变更
- 需要确保文件顶部已有 TableField 的 import（检查 geography 字段使用了 @TableField，import 应该已存在。如果不存在则添加 `import com.baomidou.mybatisplus.annotation.TableField;`）
- 不修改 forces 字段的类型（保持 String）
- 不删除任何其他字段或方法
- 数据库中的 forces 列保持不变（D-10 保留列），此注解只影响 MyBatis-Plus 的 SQL 生成
  </action>
  <verify>
    <automated>cd D:/work/ai/AI-Factory/ai-factory-backend && mvn compile -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - NovelWorldview.java 中 forces 字段上方有 @TableField(exist = false) 注解
    - NovelWorldview.java 中 forces 字段仍为 private String forces
    - NovelWorldview.java 中 geography 字段的 @TableField(exist = false) 注解保持不变
    - 文件中其他字段和方法未被修改
    - mvn compile 编译通过（返回码 0）
  </acceptance_criteria>
  <done>NovelWorldview.java 的 forces 字段已标记为 @TableField(exist = false)，编译通过，与 geography 字段的 transient 模式一致（per D-11）</done>
</task>

</tasks>

<verification>
1. `cd D:/work/ai/AI-Factory/ai-factory-backend && mvn compile -q` 编译通过
2. `grep "@TableField(exist = false)" ai-factory-backend/src/main/java/com/aifactory/entity/NovelWorldview.java` 匹配到 2 行（geography + forces）
3. `grep "@TableName" ai-factory-backend/src/main/java/com/aifactory/entity/NovelFaction*.java` 返回 4 行（4 个实体）
4. `grep "@Mapper" ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFaction*Mapper.java` 返回 4 行（4 个 Mapper）
</verification>

<success_criteria>
- 3 个关联表实体（NovelFactionRegion、NovelFactionCharacter、NovelFactionRelation）编译通过
- 3 个关联 Mapper（NovelFactionRegionMapper、NovelFactionCharacterMapper、NovelFactionRelationMapper）编译通过
- NovelWorldview.java 的 forces 字段标记为 @TableField(exist = false)，geography 字段不变
- 全部文件 mvn compile 通过
</success_criteria>

<output>
After completion, create `.planning/phases/01-数据基础/01-02-SUMMARY.md`
</output>
