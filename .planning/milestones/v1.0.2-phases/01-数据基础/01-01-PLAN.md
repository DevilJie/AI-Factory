---
phase: 01-数据基础
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/faction_migration.sql
  - ai-factory-backend/src/main/java/com/aifactory/entity/NovelFaction.java
  - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionMapper.java
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-07

must_haves:
  truths:
    - "sql/faction_migration.sql 包含 4 张表的完整 CREATE TABLE 语句，可执行不报错"
    - "novel_faction 表 DDL 包含 id、parent_id、deep、sort_order、project_id、name、type、core_power_system、description、create_time、update_time 字段"
    - "type 和 core_power_system 列允许 NULL（NULL DEFAULT NULL）"
    - "NovelFaction.java 实体类字段与 DDL 列名通过驼峰映射一一对应"
    - "NovelFaction.java 包含 @TableField(exist=false) children 字段"
    - "NovelFactionMapper.java 继承 BaseMapper 并标注 @Mapper"
  artifacts:
    - path: "sql/faction_migration.sql"
      provides: "4 张势力相关表的 DDL 迁移脚本"
      contains: "CREATE TABLE `novel_faction`"
    - path: "ai-factory-backend/src/main/java/com/aifactory/entity/NovelFaction.java"
      provides: "势力树形实体类"
      contains: "@TableName(\"novel_faction\")"
    - path: "ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionMapper.java"
      provides: "势力 Mapper 接口"
      contains: "BaseMapper<NovelFaction>"
  key_links:
    - from: "NovelFaction.java"
      to: "novel_faction table"
      via: "@TableName annotation"
      pattern: "@TableName\\(\"novel_faction\"\\)"
    - from: "NovelFactionMapper.java"
      to: "NovelFaction.java"
      via: "BaseMapper generic type"
      pattern: "BaseMapper<NovelFaction>"
---

<objective>
创建 SQL 迁移脚本和势力树形实体，建立数据层基础。

Purpose: 数据库 DDL 和核心实体是所有后续开发的基石。faction_migration.sql 定义 4 张新表的完整结构，NovelFaction 实体映射主势力表，NovelFactionMapper 提供基础数据库操作接口。

Output: sql/faction_migration.sql (4 张表 DDL)、NovelFaction.java、NovelFactionMapper.java
</objective>

<execution_context>
@D:/work/ai/AI-Factory/.claude/get-shit-done/workflows/execute-plan.md
@D:/work/ai/AI-Factory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-数据基础/01-CONTEXT.md
@.planning/phases/01-数据基础/01-RESEARCH.md
@.planning/phases/01-数据基础/01-VALIDATION.md

<interfaces>
<!-- Reference implementations the executor must follow exactly -->

From ai-factory-backend/src/main/java/com/aifactory/entity/NovelContinentRegion.java:
```java
@Data
@TableName("novel_continent_region")
public class NovelContinentRegion {
    @TableId(type = IdType.AUTO)
    private Long id;
    private Long parentId;
    private Integer deep;
    private Integer sortOrder;
    private Long projectId;
    private String name;
    private String description;
    private LocalDateTime createTime;
    private LocalDateTime updateTime;
    @TableField(exist = false)
    private List<NovelContinentRegion> children;
}
```

From ai-factory-backend/src/main/java/com/aifactory/mapper/NovelContinentRegionMapper.java:
```java
@Mapper
public interface NovelContinentRegionMapper extends BaseMapper<NovelContinentRegion> {
}
```

From sql/geography_migration.sql DDL pattern:
- SET NAMES utf8mb4;
- CREATE TABLE with CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci
- bigint NOT NULL AUTO_INCREMENT for id
- datetime NULL DEFAULT CURRENT_TIMESTAMP for create_time
- datetime NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP for update_time
- ENGINE = InnoDB CHARACTER SET = utf8mb4 COLLATE = utf8mb4_unicode_ci ROW_FORMAT = Dynamic
- INDEX on project_id and parent_id columns
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 faction_migration.sql 迁移脚本</name>
  <files>sql/faction_migration.sql</files>
  <read_first>
    - sql/geography_migration.sql (DDL 风格参考，SET NAMES utf8mb4、索引命名、ENGINE/CHARSET/ROW_FORMAT 规范)
    - sql/power_system_migration.sql (关联表 DDL 风格参考)
  </read_first>
  <action>
创建 sql/faction_migration.sql，参照 geography_migration.sql 的格式风格。文件包含 4 张表的 CREATE TABLE 语句。严格遵循以下规格：

**1. novel_faction 表（主势力树形表，参照 novel_continent_region 增加字段）：**
```sql
CREATE TABLE `novel_faction` (
    `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
    `parent_id` bigint NULL DEFAULT NULL COMMENT '父级ID（NULL表示根节点）',
    `deep` int NOT NULL DEFAULT 0 COMMENT '树层级深度（0=根节点）',
    `sort_order` int NOT NULL DEFAULT 0 COMMENT '同级排序（越小越靠前）',
    `project_id` bigint NOT NULL COMMENT '归属项目ID',
    `name` varchar(200) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '势力名称',
    `type` varchar(20) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL COMMENT '势力类型（ally/hostile/neutral，仅顶级设置）',
    `core_power_system` bigint NULL DEFAULT NULL COMMENT '核心力量体系ID（仅顶级设置，下级继承）',
    `description` text CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL COMMENT '势力描述',
    `create_time` datetime NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` datetime NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`) USING BTREE,
    INDEX `idx_project_id`(`project_id` ASC) USING BTREE,
    INDEX `idx_parent_id`(`parent_id` ASC) USING BTREE
) ENGINE = InnoDB CHARACTER SET = utf8mb4 COLLATE = utf8mb4_unicode_ci COMMENT = '势力表（树形结构）' ROW_FORMAT = Dynamic;
```

**2. novel_faction_region 表（势力-地区关联，参照 novel_worldview_power_system 简化）：**
```sql
CREATE TABLE `novel_faction_region` (
    `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
    `faction_id` bigint NOT NULL COMMENT '势力ID',
    `region_id` bigint NOT NULL COMMENT '地区ID（关联 novel_continent_region.id）',
    PRIMARY KEY (`id`) USING BTREE,
    INDEX `idx_faction_id`(`faction_id` ASC) USING BTREE,
    INDEX `idx_region_id`(`region_id` ASC) USING BTREE
) ENGINE = InnoDB CHARACTER SET = utf8mb4 COLLATE = utf8mb4_unicode_ci COMMENT = '势力-地区关联表' ROW_FORMAT = Dynamic;
```

**3. novel_faction_character 表（势力-人物关联）：**
```sql
CREATE TABLE `novel_faction_character` (
    `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
    `faction_id` bigint NOT NULL COMMENT '势力ID',
    `character_id` bigint NOT NULL COMMENT '人物ID（关联 novel_character.id）',
    `role` varchar(100) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL COMMENT '职位（如掌门、长老、弟子，自由文本）',
    PRIMARY KEY (`id`) USING BTREE,
    INDEX `idx_faction_id`(`faction_id` ASC) USING BTREE,
    INDEX `idx_character_id`(`character_id` ASC) USING BTREE
) ENGINE = InnoDB CHARACTER SET = utf8mb4 COLLATE = utf8mb4_unicode_ci COMMENT = '势力-人物关联表' ROW_FORMAT = Dynamic;
```

**4. novel_faction_relation 表（势力-势力关系，双向存储）：**
```sql
CREATE TABLE `novel_faction_relation` (
    `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
    `faction_id` bigint NOT NULL COMMENT '势力ID',
    `target_faction_id` bigint NOT NULL COMMENT '目标势力ID',
    `relation_type` varchar(20) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '关系类型（ally/hostile/neutral）',
    `description` varchar(500) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL COMMENT '关系描述',
    PRIMARY KEY (`id`) USING BTREE,
    INDEX `idx_faction_id`(`faction_id` ASC) USING BTREE,
    INDEX `idx_target_faction_id`(`target_faction_id` ASC) USING BTREE
) ENGINE = InnoDB CHARACTER SET = utf8mb4 COLLATE = utf8mb4_unicode_ci COMMENT = '势力-势力关系表（双向存储）' ROW_FORMAT = Dynamic;
```

关键约束（per D-01 到 D-10）：
- 文件开头加 `SET NAMES utf8mb4;`
- 不加任何 ALTER TABLE 或 DROP COLUMN（D-10 保留 forces 列）
- 不加任何 FOREIGN KEY 约束（D-08 纯逻辑关联）
- type 和 core_power_system 允许 NULL（D-02/D-05 仅顶级设置）
- description 使用 TEXT 类型（D-04）
- relation_type 使用英文编码 ally/hostile/neutral（与项目 role_type 风格一致）
- 每张表前加注释说明用途
  </action>
  <verify>
    <automated>cd D:/work/ai/AI-Factory && grep -c "CREATE TABLE" sql/faction_migration.sql | grep -q "^4$" && echo "PASS: 4 CREATE TABLE statements found" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sql/faction_migration.sql 文件存在
    - 文件包含 4 个 CREATE TABLE 语句（novel_faction、novel_faction_region、novel_faction_character、novel_faction_relation）
    - 文件开头有 SET NAMES utf8mb4;
    - novel_faction 表包含 type varchar(20) NULL DEFAULT NULL 列
    - novel_faction 表包含 core_power_system bigint NULL DEFAULT NULL 列
    - novel_faction 表包含 description text 列
    - novel_faction_region 表只有 id、faction_id、region_id 三个字段（不加 description）
    - novel_faction_character 表包含 role varchar(100) NULL DEFAULT NULL 列
    - novel_faction_relation 表包含 relation_type varchar(20) NOT NULL 列
    - novel_faction_relation 表包含 description varchar(500) 列
    - 没有任何 ALTER TABLE 或 DROP COLUMN 语句
    - 没有任何 FOREIGN KEY 约束
    - 所有表使用 ENGINE = InnoDB CHARACTER SET = utf8mb4 ROW_FORMAT = Dynamic
  </acceptance_criteria>
  <done>sql/faction_migration.sql 包含 4 张表的完整 DDL，可直接执行建表，所有列定义和约束符合 CONTEXT.md 中的决策</done>
</task>

<task type="auto">
  <name>Task 2: 创建 NovelFaction 实体和 Mapper</name>
  <files>
    - ai-factory-backend/src/main/java/com/aifactory/entity/NovelFaction.java
    - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionMapper.java
  </files>
  <read_first>
    - ai-factory-backend/src/main/java/com/aifactory/entity/NovelContinentRegion.java (树形实体模式参考，完全复用此模式)
    - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelContinentRegionMapper.java (Mapper 模式参考)
    - sql/faction_migration.sql (刚创建的 DDL，实体字段必须与此文件列名一致)
  </read_first>
  <action>
创建两个文件，严格复用 NovelContinentRegion.java 和 NovelContinentRegionMapper.java 的模式。

**NovelFaction.java** (参照 NovelContinentRegion.java，增加 type/corePowerSystem 字段)：
```java
package com.aifactory.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableField;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;

import java.time.LocalDateTime;
import java.util.List;

@Data
@TableName("novel_faction")
public class NovelFaction {

    @TableId(type = IdType.AUTO)
    private Long id;

    private Long parentId;

    private Integer deep;

    private Integer sortOrder;

    private Long projectId;

    private String name;

    /** 势力类型（ally/hostile/neutral，仅顶级设置） */
    private String type;

    /** 核心力量体系ID（仅顶级设置，下级继承） */
    private Long corePowerSystem;

    private String description;

    private LocalDateTime createTime;

    private LocalDateTime updateTime;

    /** 子势力（非数据库字段） */
    @TableField(exist = false)
    private List<NovelFaction> children;
}
```

**NovelFactionMapper.java** (参照 NovelContinentRegionMapper.java)：
```java
package com.aifactory.mapper;

import com.aifactory.entity.NovelFaction;
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface NovelFactionMapper extends BaseMapper<NovelFaction> {
}
```

关键点：
- 字段名使用驼峰命名（corePowerSystem），DDL 列名使用下划线（core_power_system），MyBatis-Plus 自动映射
- type 和 corePowerSystem 不加 NOT NULL 约束（Java 引用类型默认允许 null）
- children 字段标注 @TableField(exist = false)，与 NovelContinentRegion 模式一致
- 不添加自定义查询方法（Phase 2 按需添加）
  </action>
  <verify>
    <automated>cd D:/work/ai/AI-Factory/ai-factory-backend && mvn compile -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ai-factory-backend/src/main/java/com/aifactory/entity/NovelFaction.java 文件存在
    - NovelFaction.java 包含 @TableName("novel_faction") 注解
    - NovelFaction.java 包含字段: id(Long), parentId(Long), deep(Integer), sortOrder(Integer), projectId(Long), name(String), type(String), corePowerSystem(Long), description(String), createTime(LocalDateTime), updateTime(LocalDateTime)
    - NovelFaction.java 包含 @TableField(exist = false) private List&lt;NovelFaction&gt; children 字段
    - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionMapper.java 文件存在
    - NovelFactionMapper.java 包含 @Mapper 注解
    - NovelFactionMapper.java 继承 BaseMapper&lt;NovelFaction&gt;
    - mvn compile 编译通过（返回码 0）
  </acceptance_criteria>
  <done>NovelFaction.java 实体和 NovelFactionMapper.java 接口已创建，编译通过，字段与 faction_migration.sql DDL 一一对应</done>
</task>

</tasks>

<verification>
1. `cd D:/work/ai/AI-Factory/ai-factory-backend && mvn compile -q` 编译通过
2. `grep -c "CREATE TABLE" sql/faction_migration.sql` 返回 4
3. `grep "@TableName" ai-factory-backend/src/main/java/com/aifactory/entity/NovelFaction.java` 包含 "novel_faction"
4. `grep "BaseMapper" ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionMapper.java` 包含 "NovelFaction"
</verification>

<success_criteria>
- sql/faction_migration.sql 包含 4 张表的完整 DDL，符合所有 D-01 到 D-10 决策约束
- NovelFaction.java 编译通过，字段与 DDL 对应，包含 transient children 字段
- NovelFactionMapper.java 编译通过，继承 BaseMapper
</success_criteria>

<output>
After completion, create `.planning/phases/01-数据基础/01-01-SUMMARY.md`
</output>
