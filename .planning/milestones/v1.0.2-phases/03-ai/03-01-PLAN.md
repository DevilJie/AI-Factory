---
phase: 03-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-factory-backend/src/main/resources/db/migration/V3__faction_prompt_template.sql
autonomous: true
requirements: [AI-01, AI-02]

must_haves:
  truths:
    - "AI prompt template for worldview generation instructs AI to output structured faction XML inside <f> tags"
    - "Template references region names from <g> section and power system names from <p> section"
    - "Template specifies faction type enum (正派/反派/中立), nested hierarchy, <relation> pairs, and CDATA descriptions"
  artifacts:
    - path: "ai-factory-backend/src/main/resources/db/migration/V3__faction_prompt_template.sql"
      provides: "SQL migration updating worldview prompt template faction section"
      contains: "UPDATE ai_prompt_template_version"
  key_links:
    - from: "ai_prompt_template_version.template_content"
      to: "WorldviewTaskStrategy.buildWorldviewPrompt()"
      via: "PromptTemplateService.executeTemplate('llm_worldview_create', variables)"
      pattern: "llm_worldview_create"
---

<objective>
Update the AI worldview generation prompt template (code=llm_worldview_create) so the AI outputs structured faction XML instead of plain-text CDATA inside <f> tags.

Purpose: The AI must produce parseable faction XML with nested <faction> elements, type labels, power system references, region references, and inter-faction relations. Without this, the DOM parser in Plan 02 has nothing to parse.

Output: SQL migration script that updates the active ai_prompt_template_version row for llm_worldview_create, replacing the faction section with structured XML format instructions.
</objective>

<execution_context>
@D:/work/ai/AI-Factory/.claude/get-shit-done/workflows/execute-plan.md
@D:/work/ai/AI-Factory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai/03-CONTEXT.md
@.planning/phases/03-ai/03-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From ai-factory-backend/src/main/java/com/aifactory/entity/AiPromptTemplateVersion.java:
```java
public class AiPromptTemplateVersion {
    private Long id;
    private Long templateId;
    private Integer versionNumber;
    private String templateContent;   // The prompt text with {var} placeholders
    private String variableDefinitions;
    private String versionComment;
    private Boolean isActive;         // 1=active, only one active per template
    private Long createdBy;
    private LocalDateTime createdTime;
}
```

From WorldviewTaskStrategy.buildWorldviewPrompt():
```java
// Template code used to look up prompt
String templateCode = "llm_worldview_create";
return promptTemplateService.executeTemplate(templateCode, variables);
// variables: projectDescription, storyTone, novelType, tagsSection
```

Locked XML format per D-01 through D-05:
```xml
<f>
  <faction>
    <n>紫阳宗</n>
    <type>正派</type>
    <power>仙道</power>
    <regions>东域,北域</regions>
    <d><![CDATA[正道大宗，以剑道著称]]></d>
    <relation><target>魔门</target><type>敌对</type></relation>
    <faction>
      <n>凌霄剑派</n>
      <d><![CDATA[紫阳宗下属剑派]]></d>
    </faction>
  </faction>
</f>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL migration to update faction prompt template</name>
  <files>ai-factory-backend/src/main/resources/db/migration/V3__faction_prompt_template.sql</files>
  <read_first>
    - ai-factory-backend/src/main/java/com/aifactory/service/task/impl/WorldviewTaskStrategy.java (lines 289-305: buildWorldviewPrompt method to understand template variables and code)
    - ai-factory-backend/src/main/java/com/aifactory/entity/AiPromptTemplateVersion.java (template entity structure)
    - .planning/phases/03-ai/03-CONTEXT.md (D-01 through D-05, D-11, D-12: locked XML format decisions)
  </read_first>
  <action>
Create a SQL migration file `ai-factory-backend/src/main/resources/db/migration/V3__faction_prompt_template.sql` that updates the active prompt template for code=`llm_worldview_create`.

The migration must:
1. Find the active template version for `llm_worldview_create` and update its `template_content` column
2. Replace ONLY the faction-related instructions in the template. The current template has a section instructing AI to output `<f><![CDATA[纯文本势力描述]]></f>`. Replace that section with structured XML format instructions.
3. The new faction section must instruct the AI to:
   - Output `<f>` as the outer container (per D-01)
   - Use nested `<faction>` tags for hierarchy, with child factions nested inside parent factions (per D-01)
   - Each top-level `<faction>` must include: `<n>` name, `<type>` (正派/反派/中立, per D-05, D-11), `<power>` power system name matching names from the `<p>` section (per D-12, AI-02), `<regions>` comma-separated region names matching names from the `<g>` section (per D-03, D-12, AI-02), `<d>` description wrapped in CDATA (per D-02)
   - Child factions only need `<n>` and `<d>` -- they inherit type and power from root (per D-05)
   - Inter-faction relations use `<relation>` tag pairs containing `<target>` (target faction name) and `<type>` (盟友/敌对/中立) (per D-04)
   - All data in child elements, NOT attributes (per D-02)
   - Include the complete XML example from D-01/D-02 in the instructions
   - Explicitly instruct AI to reuse EXACT names from `<p>` and `<g>` sections for `<power>` and `<regions>` references (per D-12)

IMPORTANT: This task requires reading the CURRENT template content from the database first. Since we cannot connect to the DB at planning time, the SQL migration must use a strategy that works regardless of exact current content. Approach:
- Use `UPDATE ai_prompt_template_version SET template_content = REPLACE(template_content, '{old_faction_section}', '{new_faction_section}') WHERE ... AND is_active = 1` if the old text can be identified
- OR: If the template is not too large, provide the complete updated `template_content` as a single string in the UPDATE statement
- The WHERE clause should match: `template_id = (SELECT id FROM ai_prompt_template WHERE code = 'llm_worldview_create')` AND `is_active = 1`

The new faction instruction text (in Chinese, matching the existing template language) should read approximately:

```
【势力阵营】请按以下XML格式输出势力信息：
外层使用 <f> 标签包裹，内部每个势力用 <faction> 标签表示。
子势力通过嵌套 <faction> 标签表达层级关系。
每个顶级势力包含以下子元素：
- <n> 势力名称
- <type> 势力类型（正派/反派/中立）
- <power> 力量体系名称（必须与上面<p>标签中输出的力量体系名称完全一致）
- <regions> 活动地区（逗号分隔，名称必须与上面<g>标签中输出的地区名称完全一致）
- <d> 势力描述（用CDATA包裹）
- <relation> 可选，势力间关系，包含 <target> 目标势力名称 和 <type> 关系类型（盟友/敌对/中立）
子势力只需 <n> 和 <d>，类型和力量体系继承自顶级势力。

示例：
<f>
  <faction>
    <n>紫阳宗</n>
    <type>正派</type>
    <power>仙道</power>
    <regions>东域,北域</regions>
    <d><![CDATA[正道大宗，以剑道著称]]></d>
    <relation><target>魔门</target><type>敌对</type></relation>
    <faction>
      <n>凌霄剑派</n>
      <d><![CDATA[紫阳宗下属剑派]]></d>
    </faction>
  </faction>
</f>

规则：
1. 顶级势力必须设置type（正派/反派/中立）和power
2. 子势力不写type和power，自动继承顶级势力
3. <power>中的名称必须与<p>中的力量体系名称完全一致
4. <regions>中的名称必须与<g>中的地区名称完全一致
5. 每个势力应有独特且符合题材的名称
6. 势力间关系使用<relation>标签对表示
```

Also update the `version_comment` to note this change: e.g., '势力部分从纯文本改为结构化XML格式'
  </action>
  <verify>
    <automated>cd ai-factory-backend && test -f src/main/resources/db/migration/V3__faction_prompt_template.sql && grep -q "llm_worldview_create" src/main/resources/db/migration/V3__faction_prompt_template.sql && grep -q "faction" src/main/resources/db/migration/V3__faction_prompt_template.sql && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File ai-factory-backend/src/main/resources/db/migration/V3__faction_prompt_template.sql exists
    - File contains UPDATE statement targeting ai_prompt_template_version table
    - File references llm_worldview_create template code
    - File contains the structured faction XML example with <faction>, <n>, <type>, <power>, <regions>, <d>, <relation> tags
    - File instructs AI to match names from <p> and <g> sections
    - File specifies type enum as 正派/反派/中立
  </acceptance_criteria>
  <done>
    SQL migration file created that updates the worldview prompt template to produce structured faction XML with nested hierarchy, type/power/region references, and relation pairs. Migration is idempotent (can be run safely). Addresses AI-01 (prompt template update) and AI-02 (name consistency in references).
  </done>
</task>

</tasks>

<verification>
1. Migration file exists and contains valid SQL
2. SQL references llm_worldview_create template
3. New template content includes structured XML format for factions
4. Template instructs AI to output <f> container with nested <faction> elements
5. Template specifies name consistency requirements for <power> and <regions> references
</verification>

<success_criteria>
- SQL migration file committed
- Template content includes faction XML format matching D-01 through D-05 decisions
- Template references regions and power systems by name (AI-02)
- Type enum specified as 正派/反派/中立 (D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai/03-01-SUMMARY.md`
</output>
