---
phase: 02-api
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - ai-factory-backend/src/main/java/com/aifactory/controller/FactionController.java
autonomous: true
requirements: [BACK-07, BACK-08, BACK-09, BACK-10]

must_haves:
  truths:
    - "GET /api/novel/{projectId}/faction/tree returns Result<List<NovelFaction>> with full tree"
    - "POST /api/novel/{projectId}/faction/save creates a faction and returns Result<NovelFaction>"
    - "PUT /api/novel/{projectId}/faction/update updates a faction and returns Result<NovelFaction>"
    - "DELETE /api/novel/{projectId}/faction/{id} cascading-deletes faction and returns Result<Void>"
    - "CRUD endpoints for faction-region, faction-character, and faction-relation associations all return Result<T>"
  artifacts:
    - path: "ai-factory-backend/src/main/java/com/aifactory/controller/FactionController.java"
      provides: "REST API controller for faction tree CRUD + 3 association table management"
      exports: ["getTree", "list", "add", "update", "delete", "listRegions", "addRegion", "deleteRegion", "listCharacters", "addCharacter", "deleteCharacter", "listRelations", "addRelation", "deleteRelation"]
      min_lines: 120
  key_links:
    - from: "FactionController"
      to: "FactionService"
      via: "@Autowired injection"
      pattern: "factionService\\."
    - from: "FactionController"
      to: "Result<T>"
      via: "All endpoints return Result.ok() or Result.error()"
      pattern: "Result\\.(ok|error)"
---

<objective>
Create FactionController with all REST endpoints for faction tree CRUD and 3 association table management APIs, following ContinentRegionController pattern.

Purpose: Exposes faction CRUD and association management as RESTful APIs for frontend consumption (Phase 4/5) and AI integration (Phase 3).

Output: FactionController.java with 14 endpoints (5 faction CRUD + 3x3 association CRUD)
</objective>

<execution_context>
@D:/work/ai/AI-Factory/.claude/get-shit-done/workflows/execute-plan.md
@D:/work/ai/AI-Factory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api/02-01-SUMMARY.md
@.planning/phases/02-api/02-CONTEXT.md

<interfaces>
<!-- Key types the executor needs from Plan 01 output. -->

From ai-factory-backend/src/main/java/com/aifactory/service/FactionService.java (created by Plan 01):
```java
public interface FactionService {
    List<NovelFaction> getTreeByProjectId(Long projectId);
    List<NovelFaction> listByProjectId(Long projectId);
    NovelFaction addFaction(NovelFaction faction);
    NovelFaction updateFaction(NovelFaction faction);
    void deleteFaction(Long id);
    void saveTree(Long projectId, List<NovelFaction> rootNodes);
    void deleteByProjectId(Long projectId);
    String buildFactionText(Long projectId);
    void fillForces(NovelWorldview worldview);
}
```

From ai-factory-backend/src/main/java/com/aifactory/controller/ContinentRegionController.java (reference pattern):
```java
@Slf4j
@RestController
@RequestMapping("/api/novel/{projectId}/continent-region")
@Tag(name = "大陆区域", description = "地理区域树形结构管理API")
public class ContinentRegionController {
    @Autowired
    private ContinentRegionService continentRegionService;

    @GetMapping("/tree")
    public Result<List<NovelContinentRegion>> getTree(@PathVariable Long projectId) { ... }

    @PostMapping("/save")
    public Result<NovelContinentRegion> add(@PathVariable Long projectId, @RequestBody NovelContinentRegion region) { ... }

    @PutMapping("/update")
    public Result<NovelContinentRegion> update(@PathVariable Long projectId, @RequestBody NovelContinentRegion region) { ... }

    @DeleteMapping("/{id}")
    public Result<Void> delete(@PathVariable Long projectId, @PathVariable Long id) { ... }
}
```

From ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionRegion.java:
```java
@Data @TableName("novel_faction_region")
public class NovelFactionRegion {
    @TableId(type = IdType.AUTO) private Long id;
    private Long factionId;
    private Long regionId;
}
```

From ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionCharacter.java:
```java
@Data @TableName("novel_faction_character")
public class NovelFactionCharacter {
    @TableId(type = IdType.AUTO) private Long id;
    private Long factionId;
    private Long characterId;
    private String role;
}
```

From ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionRelation.java:
```java
@Data @TableName("novel_faction_relation")
public class NovelFactionRelation {
    @TableId(type = IdType.AUTO) private Long id;
    private Long factionId;
    private Long targetFactionId;
    private String relationType;
    private String description;
}
```

From ai-factory-backend/src/main/java/com/aifactory/response/Result.java:
```java
public class Result<T> {
    private Integer code;
    private String msg;
    private T data;
    private Boolean ok;
    public static <T> Result<T> ok() { ... }
    public static <T> Result<T> ok(T data) { ... }
    public static <T> Result<T> error(String msg) { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FactionController with faction tree CRUD + 3 association management endpoint groups</name>
  <files>
    ai-factory-backend/src/main/java/com/aifactory/controller/FactionController.java
  </files>
  <read_first>
    - ai-factory-backend/src/main/java/com/aifactory/controller/ContinentRegionController.java (reference pattern to clone)
    - ai-factory-backend/src/main/java/com/aifactory/service/FactionService.java (service interface from Plan 01)
    - ai-factory-backend/src/main/java/com/aifactory/entity/NovelFaction.java
    - ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionRegion.java
    - ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionCharacter.java
    - ai-factory-backend/src/main/java/com/aifactory/entity/NovelFactionRelation.java
    - ai-factory-backend/src/main/java/com/aifactory/response/Result.java
    - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionRegionMapper.java
    - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionCharacterMapper.java
    - ai-factory-backend/src/main/java/com/aifactory/mapper/NovelFactionRelationMapper.java
  </read_first>
  <action>
    Create FactionController.java in `com.aifactory.controller` package. Follow ContinentRegionController.java pattern exactly for annotations, error handling, and response wrapping.

    **Class-level annotations (per D-07):**
    ```java
    @Slf4j
    @RestController
    @RequestMapping("/api/novel/{projectId}/faction")
    @Tag(name = "势力管理", description = "势力树形结构管理API")
    ```
    NOTE: Route prefix is `/api/novel/{projectId}/faction` (per D-07, matching ContinentRegionController pattern but with "faction" instead of "continent-region").

    **Dependency injection:**
    ```java
    @Autowired
    private FactionService factionService;
    @Autowired
    private NovelFactionRegionMapper factionRegionMapper;
    @Autowired
    private NovelFactionCharacterMapper factionCharacterMapper;
    @Autowired
    private NovelFactionRelationMapper factionRelationMapper;
    ```

    **Faction Tree CRUD endpoints (BACK-07, per D-07):**

    1. `GET /tree` -- getTree(@PathVariable Long projectId)
       - Returns Result.ok(factionService.getTreeByProjectId(projectId))
       - @Operation(summary = "获取势力树", description = "获取指定项目的势力树形结构")
       - Error: Result.error("获取势力树失败: " + e.getMessage())

    2. `GET /list` -- list(@PathVariable Long projectId)
       - Returns Result.ok(factionService.listByProjectId(projectId))
       - @Operation(summary = "获取势力列表", description = "获取指定项目的所有势力平铺列表")
       - Error: Result.error("获取势力列表失败: " + e.getMessage())

    3. `POST /save` -- add(@PathVariable Long projectId, @RequestBody NovelFaction faction)
       - Set faction.setProjectId(projectId)
       - Returns Result.ok(factionService.addFaction(faction))
       - @Operation(summary = "新增势力节点", description = "新增一个势力节点，自动计算层级和排序")
       - Error: Result.error("新增势力失败: " + e.getMessage())

    4. `PUT /update` -- update(@PathVariable Long projectId, @RequestBody NovelFaction faction)
       - Set faction.setProjectId(projectId)
       - Returns Result.ok(factionService.updateFaction(faction))
       - @Operation(summary = "更新势力节点", description = "更新势力节点的名称、描述等信息")
       - Error: Result.error("更新势力失败: " + e.getMessage())

    5. `DELETE /{id}` -- delete(@PathVariable Long projectId, @PathVariable Long id)
       - Calls factionService.deleteFaction(id)
       - Returns Result.ok()
       - @Operation(summary = "删除势力节点", description = "删除势力节点及其所有子节点（级联删除）")
       - Error: Result.error("删除势力失败: " + e.getMessage())

    **Faction-Region Association endpoints (BACK-08, per D-08):**

    6. `GET /{factionId}/regions` -- listRegions(@PathVariable Long projectId, @PathVariable Long factionId)
       - Query factionRegionMapper.selectList where factionId matches
       - Returns Result.ok(list)
       - @Operation(summary = "获取势力关联地区")

    7. `POST /{factionId}/regions` -- addRegion(@PathVariable Long projectId, @PathVariable Long factionId, @RequestBody NovelFactionRegion association)
       - Set association.setFactionId(factionId)
       - factionRegionMapper.insert(association)
       - Returns Result.ok(association)
       - @Operation(summary = "添加势力地区关联")

    8. `DELETE /{factionId}/regions/{id}` -- deleteRegion(@PathVariable Long projectId, @PathVariable Long factionId, @PathVariable Long id)
       - factionRegionMapper.deleteById(id)
       - Returns Result.ok()
       - @Operation(summary = "删除势力地区关联")

    **Faction-Character Association endpoints (BACK-09, per D-08):**

    9. `GET /{factionId}/characters` -- listCharacters(@PathVariable Long projectId, @PathVariable Long factionId)
       - Query factionCharacterMapper.selectList where factionId matches
       - Returns Result.ok(list)
       - @Operation(summary = "获取势力关联人物")

    10. `POST /{factionId}/characters` -- addCharacter(@PathVariable Long projectId, @PathVariable Long factionId, @RequestBody NovelFactionCharacter association)
        - Set association.setFactionId(factionId)
        - factionCharacterMapper.insert(association)
        - Returns Result.ok(association)
        - @Operation(summary = "添加势力人物关联")

    11. `DELETE /{factionId}/characters/{id}` -- deleteCharacter(@PathVariable Long projectId, @PathVariable Long factionId, @PathVariable Long id)
        - factionCharacterMapper.deleteById(id)
        - Returns Result.ok()
        - @Operation(summary = "删除势力人物关联")

    **Faction-Faction Relation endpoints (BACK-10, per D-08):**

    12. `GET /{factionId}/relations` -- listRelations(@PathVariable Long projectId, @PathVariable Long factionId)
        - Query factionRelationMapper.selectList where factionId matches (using LambdaQueryWrapper)
        - Returns Result.ok(list)
        - @Operation(summary = "获取势力关系列表")

    13. `POST /{factionId}/relations` -- addRelation(@PathVariable Long projectId, @PathVariable Long factionId, @RequestBody NovelFactionRelation relation)
        - Set relation.setFactionId(factionId)
        - factionRelationMapper.insert(relation)
        - Returns Result.ok(relation)
        - @Operation(summary = "添加势力关系")

    14. `DELETE /{factionId}/relations/{id}` -- deleteRelation(@PathVariable Long projectId, @PathVariable Long factionId, @PathVariable Long id)
        - factionRelationMapper.deleteById(id)
        - Returns Result.ok()
        - @Operation(summary = "删除势力关系")

    **Error handling pattern for ALL endpoints (clone from ContinentRegionController):**
    ```java
    try {
        // business logic
        return Result.ok(...);
    } catch (Exception e) {
        log.error("操作描述失败，projectId={}", projectId, e);
        return Result.error("操作描述失败: " + e.getMessage());
    }
    ```

    **Import requirements:**
    - com.aifactory.entity.NovelFaction
    - com.aifactory.entity.NovelFactionRegion
    - com.aifactory.entity.NovelFactionCharacter
    - com.aifactory.entity.NovelFactionRelation
    - com.aifactory.response.Result
    - com.aifactory.service.FactionService
    - com.aifactory.mapper.NovelFactionRegionMapper
    - com.aifactory.mapper.NovelFactionCharacterMapper
    - com.aifactory.mapper.NovelFactionRelationMapper
    - com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper
    - io.swagger.v3.oas.annotations.Operation
    - io.swagger.v3.oas.annotations.tags.Tag
    - lombok.extern.slf4j.Slf4j
    - org.springframework.beans.factory.annotation.Autowired
    - org.springframework.web.bind.annotation.*
    - java.util.List

    Association list queries use LambdaQueryWrapper pattern:
    ```java
    factionRegionMapper.selectList(
        new LambdaQueryWrapper<NovelFactionRegion>()
            .eq(NovelFactionRegion::getFactionId, factionId));
    ```
  </action>
  <verify>
    <automated>cd ai-factory-backend && mvn compile -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - FactionController.java exists with 14 endpoints
    - Route prefix is /api/novel/{projectId}/faction (per D-07)
    - All endpoints return Result<T> with try-catch error handling
    - All endpoints have @Operation annotations
    - Association endpoints use direct mapper calls for simple CRUD (per D-08)
    - mvn compile succeeds
  </done>
  <acceptance_criteria>
    - File exists: ai-factory-backend/src/main/java/com/aifactory/controller/FactionController.java
    - Contains "@RequestMapping(\"/api/novel/{projectId}/faction\")"
    - Contains "@Tag(name = \"势力管理\""
    - Contains "@GetMapping(\"/tree\")"
    - Contains "@PostMapping(\"/save\")"
    - Contains "@PutMapping(\"/update\")"
    - Contains "@DeleteMapping(\"/{id}\")"
    - Contains "@GetMapping(\"/{factionId}/regions\")"
    - Contains "@PostMapping(\"/{factionId}/regions\")"
    - Contains "@DeleteMapping(\"/{factionId}/regions/{id}\")"
    - Contains "@GetMapping(\"/{factionId}/characters\")"
    - Contains "@PostMapping(\"/{factionId}/characters\")"
    - Contains "@DeleteMapping(\"/{factionId}/characters/{id}\")"
    - Contains "@GetMapping(\"/{factionId}/relations\")"
    - Contains "@PostMapping(\"/{factionId}/relations\")"
    - Contains "@DeleteMapping(\"/{factionId}/relations/{id}\")"
    - Contains "Result.ok()" at least 14 times
    - Contains "Result.error(" at least 14 times
    - Contains "factionRegionMapper.selectList"
    - Contains "factionCharacterMapper.selectList"
    - Contains "factionRelationMapper.selectList"
    - `cd ai-factory-backend && mvn compile -q` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `cd ai-factory-backend && mvn compile -q` -- FactionController compiles without errors
2. All 14 endpoints defined with correct HTTP methods and paths
3. All endpoints use Result<T> response wrapper with try-catch
4. Route prefix matches /api/novel/{projectId}/faction
</verification>

<success_criteria>
- FactionController.java has 14 endpoints (5 faction + 3 region + 3 character + 3 relation)
- All endpoints return Result<T> with Swagger @Operation annotations
- Association CRUD uses direct mapper injection (per D-08)
- mvn compile exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-api/02-02-SUMMARY.md`
</output>
