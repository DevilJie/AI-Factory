---
phase: 04-前端树组件
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - ai-factory-frontend/src/views/Project/Detail/WorldSetting.vue
autonomous: true
requirements:
  - UI-03

must_haves:
  truths:
    - "WorldSetting.vue 页面显示 FactionTree 组件替代势力 textarea"
    - "原有的势力 textarea 不再出现在页面上"
    - "AI 生成世界观后 FactionTree 自动刷新显示新数据"
    - "页面刷新后恢复中的 AI 生成任务完成时 FactionTree 也会刷新"
    - "AI 生成中 FactionTree 的所有操作按钮被禁用"
  artifacts:
    - path: "ai-factory-frontend/src/views/Project/Detail/WorldSetting.vue"
      provides: "World setting page with FactionTree integrated"
      contains: "FactionTree"
  key_links:
    - from: "ai-factory-frontend/src/views/Project/Detail/WorldSetting.vue"
      to: "ai-factory-frontend/src/views/Project/Detail/components/FactionTree.vue"
      via: "import FactionTree + ref + refresh() calls"
      pattern: "import FactionTree|factionTreeRef.*refresh"
    - from: "WorldSetting.vue handleGenerate()"
      to: "FactionTree.refresh()"
      via: "factionTreeRef.value?.refresh() after loadData"
      pattern: "factionTreeRef.*refresh"
---

<objective>
Integrate FactionTree component into WorldSetting.vue, replacing the forces textarea.

Purpose: Complete the UI-03 requirement -- replace the plain-text forces textarea with the structured FactionTree component, ensuring AI generation triggers refresh.

Output: Modified WorldSetting.vue with FactionTree replacing textarea, refresh calls added in both generation paths.
</objective>

<execution_context>
@D:/work/ai/AI-Factory/.claude/get-shit-done/workflows/execute-plan.md
@D:/work/ai/AI-Factory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-前端树组件/04-CONTEXT.md
@.planning/phases/04-前端树组件/04-RESEARCH.md

<!-- Prior plan summary for interface context -->
@.planning/phases/04-前端树组件/04-01-SUMMARY.md

<interfaces>
<!-- FactionTree component (from Plan 01) -->
// Props: projectId: string, disabled: boolean
// Exposed: refresh() -> void
// Import path: ./components/FactionTree.vue

<!-- GeographyTree integration pattern (current WorldSetting.vue) -->
import GeographyTree from './components/GeographyTree.vue'
const geographyTreeRef = ref()
// Used as: <GeographyTree ref="geographyTreeRef" :project-id="projectId()" :disabled="generating" />
// Refresh: geographyTreeRef.value?.refresh()

<!-- WorldSetting.vue current state -->
// Lines 1-16: imports
// Line 16: import GeographyTree from './components/GeographyTree.vue'
// Line 33: const geographyTreeRef = ref()
// Line 153: geographyTreeRef.value?.refresh()  (in handleGenerate)
// Line 188: geographyTreeRef.value?.refresh()  (in restoreGeneratingState)
// Lines 326-327: GeographyTree usage in template
// Lines 329-341: Forces textarea block (TO BE REPLACED)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace forces textarea with FactionTree in WorldSetting.vue</name>
  <files>ai-factory-frontend/src/views/Project/Detail/WorldSetting.vue</files>
  <read_first>
    - ai-factory-frontend/src/views/Project/Detail/WorldSetting.vue (ENTIRE file -- must see exact line numbers for edits)
    - ai-factory-frontend/src/views/Project/Detail/components/GeographyTree.vue (reference pattern for how GeographyTree is integrated)
  </read_first>
  <action>
Make these specific edits to `ai-factory-frontend/src/views/Project/Detail/WorldSetting.vue`:

**1. Add FactionTree import (after line 16, the GeographyTree import):**
Add this line immediately after `import GeographyTree from './components/GeographyTree.vue'`:
```
import FactionTree from './components/FactionTree.vue'
```

**2. Add factionTreeRef (after line 33, the geographyTreeRef):**
Add this line immediately after `const geographyTreeRef = ref()`:
```
const factionTreeRef = ref()
```

**3. Add refresh call in handleGenerate() (after line 153):**
Immediately after `geographyTreeRef.value?.refresh()` on line 153, add:
```
    factionTreeRef.value?.refresh()
```
This must be BEFORE the `success('AI生成世界观成功')` line on line 154.

**4. Add refresh call in restoreGeneratingState() (after line 188):**
Immediately after `geographyTreeRef.value?.refresh()` on line 188, add:
```
      factionTreeRef.value?.refresh()
```
This must be BEFORE the `success('AI生成世界观完成')` line on line 189.

**5. Replace the forces textarea block (lines 329-342) with FactionTree component:**

REMOVE the entire block from line 329 to line 342 (the comment "<!-- 第四行：势力阵营 -->" through the closing `</div>`):
```html
        <!-- 第四行：势力阵营 -->
        <div class="grid grid-cols-1 gap-6">
          <div class="bg-white dark:bg-gray-800 rounded-xl p-6 shadow-sm">
            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-3">
              势力阵营
            </label>
            <textarea
              v-model="formData.forces"
              rows="4"
              :disabled="generating"
              placeholder="描述世界中的主要势力、组织、阵营及其相互关系..."
              class="w-full px-4 py-3 bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-600 rounded-lg text-gray-900 dark:text-white placeholder-gray-400 resize-none focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent disabled:opacity-50"
            ></textarea>
          </div>
        </div>
```

REPLACE with this single line (per D-05 -- completely replace textarea, full row layout matching GeographyTree on line 326):
```html
        <!-- 第四行：势力阵营（树形组件） -->
        <FactionTree ref="factionTreeRef" :project-id="projectId()" :disabled="generating" />
```

The FactionTree component has its own card wrapper (bg-white rounded-xl p-6 shadow-sm), so it does NOT need an outer div wrapper. It should be a direct child of the `<div class="space-y-6">` container, exactly like GeographyTree on line 326.

**Summary of all changes:**
- +1 import line
- +1 ref declaration
- +2 refresh calls (handleGenerate + restoreGeneratingState)
- -14 lines (textarea block removed)
- +2 lines (comment + FactionTree component tag)
  </action>
  <verify>
    <automated>cd ai-factory-frontend && npx tsc --noEmit 2>&1 | head -10; echo "---"; grep -n "FactionTree" ai-factory-frontend/src/views/Project/Detail/WorldSetting.vue; echo "---"; grep -n "factionTreeRef" ai-factory-frontend/src/views/Project/Detail/WorldSetting.vue; echo "---"; grep -c "formData.forces" ai-factory-frontend/src/views/Project/Detail/WorldSetting.vue</automated>
  </verify>
  <done>
    - FactionTree imported from './components/FactionTree.vue'
    - factionTreeRef declared as ref()
    - factionTreeRef.value?.refresh() present in BOTH handleGenerate() and restoreGeneratingState()
    - Forces textarea block (lines 329-341) completely removed
    - <FactionTree> component tag present in template with ref="factionTreeRef" :project-id :disabled
    - formData.forces no longer referenced in template (only in formData initialization, which is fine)
    - TypeScript compiles without errors
    - GeographyTree integration pattern preserved exactly (not modified)
  </done>
</task>

</tasks>

<verification>
1. TypeScript: `cd ai-factory-frontend && npx tsc --noEmit` passes
2. No textarea: `grep -c "formData.forces" ai-factory-frontend/src/views/Project/Detail/WorldSetting.vue` should return 1 (only in formData init, not template)
3. FactionTree present: `grep "FactionTree" ai-factory-frontend/src/views/Project/Detail/WorldSetting.vue` returns import + ref + template usage
4. Refresh calls: `grep -c "factionTreeRef.*refresh" ai-factory-frontend/src/views/Project/Detail/WorldSetting.vue` returns 2
5. GeographyTree unchanged: `grep -c "geographyTreeRef.*refresh" ai-factory-frontend/src/views/Project/Detail/WorldSetting.vue` returns 2
</verification>

<success_criteria>
- Forces textarea completely replaced by FactionTree component in WorldSetting.vue
- FactionTree refreshes after AI worldview generation (both fresh and restored states)
- GeographyTree integration untouched
- No duplicate UI elements (no textarea AND tree showing simultaneously)
- TypeScript compiles cleanly
- D-05 (complete textarea replacement) and D-06 (AI generation auto-refresh) implemented
</success_criteria>

<output>
After completion, create `.planning/phases/04-前端树组件/04-02-SUMMARY.md`
</output>
