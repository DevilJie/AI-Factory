---
phase: 06-独立提示词模板
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-factory-backend/src/main/resources/db/migration/V4__independent_prompt_templates.sql
autonomous: true
requirements:
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03
  - PROMPT-04

must_haves:
  truths:
    - "地理环境独立模板 llm_geography_create 存在，仅包含地理环境生成指令和 <g> 格式说明"
    - "力量体系独立模板 llm_power_system_create 存在，仅包含力量体系生成指令和 <p> 格式说明"
    - "阵营势力独立模板 llm_faction_create 存在，包含 {geographyContext} 和 {powerSystemContext} 变量占位符"
    - "统一世界观模板 llm_worldview_create 已精简，仅包含 <t>/<b>/<l>/<r> 四个字段的格式说明"
  artifacts:
    - path: "ai-factory-backend/src/main/resources/db/migration/V4__independent_prompt_templates.sql"
      provides: "Flyway migration creating 3 new templates and updating 1 existing template"
      min_lines: 100
      contains: "llm_geography_create"
  key_links:
    - from: "V4__independent_prompt_templates.sql"
      to: "ai_prompt_template table"
      via: "INSERT INTO ai_prompt_template"
      pattern: "INSERT INTO ai_prompt_template.*llm_geography_create"
    - from: "V4__independent_prompt_templates.sql"
      to: "ai_prompt_template_version table"
      via: "INSERT INTO ai_prompt_template_version"
      pattern: "INSERT INTO ai_prompt_template_version"
---

<objective>
Create a Flyway V4 SQL migration that creates 3 independent prompt templates (geography, power system, faction) by extracting content from the existing unified worldview template, and updates the unified template to only retain world-type/background/timeline/rules sections.

Purpose: Each worldview submodule gets its own AI prompt template, enabling independent generation of geography, power systems, and factions without regenerating the entire worldview. This is the data foundation for Phase 7's independent generation APIs.

Output: Single SQL migration file V4__independent_prompt_templates.sql
</objective>

<execution_context>
@D:/work/ai/AI-Factory/.claude/get-shit-done/workflows/execute-plan.md
@D:/work/ai/AI-Factory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-独立提示词模板/06-CONTEXT.md
@.planning/phases/06-独立提示词模板/06-RESEARCH.md

@ai-factory-backend/src/main/resources/db/migration/V3__faction_prompt_template.sql
@ai-factory-backend/src/main/java/com/aifactory/entity/AiPromptTemplate.java
@ai-factory-backend/src/main/java/com/aifactory/entity/AiPromptTemplateVersion.java

<interfaces>
<!-- Key schema facts the executor needs. Verified from sql/init.sql -->

ai_prompt_template table columns:
- id: bigint NOT NULL AUTO_INCREMENT
- template_code: varchar(100) NOT NULL, UNIQUE (uk_template_code)
- template_name: varchar(200) NOT NULL
- service_type: varchar(50) NOT NULL ('llm' for AI templates)
- scenario: varchar(100) NOT NULL
- current_version_id: bigint NOT NULL
- description: text NULL
- tags: varchar(500) NULL
- is_active: tinyint(1) DEFAULT 1
- is_system: tinyint(1) DEFAULT 0
- created_by: bigint NULL
- created_time: datetime DEFAULT CURRENT_TIMESTAMP
- updated_by: bigint NULL
- updated_time: datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE

ai_prompt_template_version table columns:
- id: bigint NOT NULL AUTO_INCREMENT
- template_id: bigint NOT NULL
- version_number: int NOT NULL, UNIQUE per template (uk_template_version)
- template_content: longtext NOT NULL
- variable_definitions: json NULL
- version_comment: varchar(500) NULL
- is_active: tinyint(1) DEFAULT 1
- created_by: bigint NULL
- created_time: datetime DEFAULT CURRENT_TIMESTAMP

CRITICAL: V3 migration used WHERE code = 'llm_worldview_create' but the actual column is template_code.
V4 MUST use WHERE template_code = 'llm_worldview_create'.

Variable substitution uses Hutool StrUtil.format() with {variableName} placeholders.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create V4 migration with geography and power system templates (PROMPT-01, PROMPT-02)</name>
  <files>ai-factory-backend/src/main/resources/db/migration/V4__independent_prompt_templates.sql</files>
  <read_first>
    ai-factory-backend/src/main/resources/db/migration/V3__faction_prompt_template.sql
    ai-factory-backend/src/main/java/com/aifactory/entity/AiPromptTemplate.java
    ai-factory-backend/src/main/java/com/aifactory/entity/AiPromptTemplateVersion.java
  </read_first>
  <action>
Create the V4 migration SQL file. This task creates the file and writes the first two template INSERTs. Task 2 will append the faction template INSERT and the unified template UPDATE to the same file.

The file must start with:

```sql
-- V4__independent_prompt_templates.sql
-- 世界观生成任务拆分 - 创建3个独立提示词模板，精简原有统一模板
-- 地理环境、力量体系、阵营势力各创建独立模板，原世界观模板仅保留世界类型/背景/时间线/规则

SET NAMES utf8mb4;
```

Then implement the following 3-step pattern for EACH of the two templates:

**Template 1: llm_geography_create (PROMPT-01)**

Step 1 - INSERT master record:
```sql
INSERT INTO ai_prompt_template (template_code, template_name, service_type, scenario, current_version_id, description, tags, is_active, is_system)
VALUES ('llm_geography_create', '地理环境独立生成', 'llm', 'worldview_geography_generate', 0, '独立生成地理环境设定，输出<g>标签格式XML', '地理环境,世界观,独立生成', 1, 1);
```
NOTE: current_version_id=0 is temporary; Step 3 will UPDATE it. The column is NOT NULL but AUTO_INCREMENT allows the INSERT to succeed with 0 as placeholder.

Step 2 - INSERT version record:
```sql
INSERT INTO ai_prompt_template_version (template_id, version_number, template_content, variable_definitions, version_comment, is_active)
VALUES (
  (SELECT id FROM ai_prompt_template WHERE template_code = 'llm_geography_create'),
  1,
  '...template content...',
  '...variable definitions JSON...',
  '从统一模板中提取地理环境部分',
  1
);
```

Template content for llm_geography_create must contain:
- Role definition: "你是一位资深的网文世界观架构师，擅长构建宏大、自洽、富有吸引力的虚构世界。"
- Variable injection block (per D-08):
  ```
  【项目描述】
  {projectDescription}

  【故事类型】{storyGenre}
  【故事基调】{storyTone}
  {tagsSection}
  ```
- Task instruction: "根据以上项目描述，为这部小说创建地理环境设定。请严格按照以下XML格式返回（使用简化标签节省token）："
- Output format with `<g>` root containing `<r>` elements using the NEW sub-tag format (per CONTEXT.md specifics):
  ```
  <g>
    <r><n>大陆/星球名称</n><d><![CDATA[整体描述]]></d>
      <r><n>子区域名称</n><d><![CDATA[区域描述]]></d></r>
    </r>
  </g>
  ```
  IMPORTANT: Use `<r><n>名称</n><d>描述</d></r>` format, NOT the old `<r name="名称">` format. This matches what parseSingleRegion() expects as the primary format.
- Geography-specific generation requirements extracted from V3 lines 92-96:
  - 至少生成2-3个顶级区域（大陆/星球）
  - 每个顶级区域下至少有2-3个子区域
  - 子区域可以继续嵌套更深层级
  - 名称放在<n>子标签中，描述放在<d>子标签中
  - 描述要结合世界背景，合理且详细
- Format rules:
  - 长文本内容使用CDATA标签包裹
  - 不要包含markdown代码块标记，直接返回XML
  - 不要包含任何解释或说明文字，只返回XML数据

Variable definitions JSON:
```json
[{"name":"projectDescription","type":"string","desc":"项目描述","required":true},{"name":"storyTone","type":"string","desc":"故事基调","required":true},{"name":"storyGenre","type":"string","desc":"故事类型","required":true},{"name":"tagsSection","type":"string","desc":"标签信息","required":false}]
```

Step 3 - UPDATE master record's current_version_id:
```sql
UPDATE ai_prompt_template
SET current_version_id = (SELECT MAX(id) FROM ai_prompt_template_version
  WHERE template_id = (SELECT id FROM ai_prompt_template WHERE template_code = 'llm_geography_create'))
WHERE template_code = 'llm_geography_create';
```

**Template 2: llm_power_system_create (PROMPT-02)**

Same 3-step pattern with these values:

Master record:
- template_code: 'llm_power_system_create'
- template_name: '力量体系独立生成'
- scenario: 'worldview_power_system_generate'
- description: '独立生成力量体系设定，输出<p>标签格式XML'
- tags: '力量体系,世界观,独立生成'

Template content must contain:
- Same role definition as geography
- Same variable injection block (per D-08): projectDescription, storyGenre, storyTone, tagsSection
- Task instruction: "根据以上项目描述，为这部小说创建力量体系设定。支持多套修炼体系，包含等级划分、境界划分、突破条件等详细信息。请严格按照以下XML格式返回："
- Output format with `<p>` root containing `<ss>` (system) elements, extracted from V3 lines 33-52:
  ```
  <p>
    <ss>
      <name>体系名称</name>
      <sf>能量来源</sf>
      <cr>核心资源</cr>
      <cm>修炼方式</cm>
      <d><![CDATA[体系整体描述]]></d>
      <lls>
        <ll>
          <ln>大境界名称</ln>
          <dd><![CDATA[等级描述]]></dd>
          <bc><![CDATA[突破条件]]></bc>
          <lsp>寿命范围</lsp>
          <pr><![CDATA[战力描述]]></pr>
          <la>标志性能力</la>
          <steps>
            <step>小境界</step>
          </steps>
        </ll>
      </lls>
    </ss>
  </p>
  ```
- Power system specific rules extracted from V3 lines 137-144:
  - <p>标签下可以包含多个<ss>，代表多套修炼体系
  - 每个体系的大境界要尽可能完善，小境界也要合理
  - 每个修炼体系之间相对应，要有相同的大境界数量
  - 对应大境界等级相同则实力相似
  - 如果故事类型不需要力量体系，<p>标签可以为空
  - 严禁一行文字概括小境界（如"一层到九层"），必须具体列出
- Same format rules as geography (CDATA, no markdown, no explanation)

Variable definitions JSON: same 4 variables as geography template.

  </action>
  <verify>
    <automated>test -f "ai-factory-backend/src/main/resources/db/migration/V4__independent_prompt_templates.sql" && grep -c "llm_geography_create" "ai-factory-backend/src/main/resources/db/migration/V4__independent_prompt_templates.sql" && grep -c "llm_power_system_create" "ai-factory-backend/src/main/resources/db/migration/V4__independent_prompt_templates.sql"</automated>
  </verify>
  <done>
    V4 migration file exists with:
    - INSERT for llm_geography_create master record (template_code, template_name='地理环境独立生成', service_type='llm', scenario='worldview_geography_generate')
    - INSERT for llm_geography_create version record containing <g> format with <r><n>...</n><d>...</d></r> sub-tag format
    - UPDATE setting current_version_id for llm_geography_create
    - INSERT for llm_power_system_create master record (template_code, template_name='力量体系独立生成', service_type='llm', scenario='worldview_power_system_generate')
    - INSERT for llm_power_system_create version record containing <p> format with <ss> structure
    - UPDATE setting current_version_id for llm_power_system_create
    - All WHERE clauses use template_code (not code)
    - Variable placeholders use {variableName} format (not ${} or {{}})
  </done>
</task>

<task type="auto">
  <name>Task 2: Add faction template INSERT and unified template UPDATE (PROMPT-03, PROMPT-04)</name>
  <files>ai-factory-backend/src/main/resources/db/migration/V4__independent_prompt_templates.sql</files>
  <read_first>
    ai-factory-backend/src/main/resources/db/migration/V4__independent_prompt_templates.sql
    ai-factory-backend/src/main/resources/db/migration/V3__faction_prompt_template.sql
  </read_first>
  <action>
APPEND to the existing V4 migration file created in Task 1. Add the faction template INSERT and the unified template UPDATE.

**Template 3: llm_faction_create (PROMPT-03)**

Same 3-step INSERT pattern as Task 1 with these values:

Master record:
- template_code: 'llm_faction_create'
- template_name: '阵营势力独立生成'
- scenario: 'worldview_faction_generate'
- description: '独立生成阵营势力设定，输出<f>标签格式XML，需要已生成的地理环境和力量体系数据作为上下文'
- tags: '阵营势力,世界观,独立生成'

Template content must contain:
- Same role definition
- Variable injection block with 6 variables (per D-09):
  ```
  【项目描述】
  {projectDescription}

  【故事类型】{storyGenre}
  【故事基调】{storyTone}
  {tagsSection}
  ```
- Context injection section for existing data (per D-04, D-05):
  ```
  以下是本项目已生成的地理环境设定（AI看到的数据格式与生成时一致）：
  <existing_geography>
  {geographyContext}
  </existing_geography>

  以下是本项目已生成的力量体系设定：
  <existing_power_systems>
  {powerSystemContext}
  </existing_power_systems>
  ```
  The geographyContext and powerSystemContext will contain the actual <g>...</g> and <p>...</p> XML from the database. The wrapper tags <existing_geography> and <existing_power_systems> differentiate "input context" from "output format".

- Task instruction: "根据以上项目描述和已有的地理环境、力量体系设定，为这部小说创建阵营势力设定。势力分布要与地理环境和力量体系紧密关联。请严格按照以下XML格式返回："
- Output format with `<f>` root, extracted from V3 lines 59-71:
  ```
  <f>
    <faction>
      <n>势力名称</n>
      <type>势力类型（正派/反派/中立）</type>
      <power>力量体系名称</power>
      <regions>活动地区（逗号分隔）</regions>
      <d><![CDATA[势力描述]]></d>
      <relation><target>目标势力名称</target><type>关系类型</type></relation>
      <faction>
        <n>子势力名称</n>
        <d><![CDATA[子势力描述]]></d>
      </faction>
    </faction>
  </f>
  ```
- Faction-specific rules extracted from V3 lines 99-131:
  - 顶级势力必须设置type和power
  - 子势力不写type和power，自动继承顶级势力
  - <power>中的名称必须与已有力量体系名称完全一致
  - <regions>中的名称必须与已有地理环境区域名称完全一致
  - 每个势力应有独特且符合题材的名称
  - 势力间关系使用<relation>标签对表示
- Same format rules (CDATA, no markdown, no explanation)

Variable definitions JSON with 6 variables (per D-09):
```json
[{"name":"projectDescription","type":"string","desc":"项目描述","required":true},{"name":"storyTone","type":"string","desc":"故事基调","required":true},{"name":"storyGenre","type":"string","desc":"故事类型","required":true},{"name":"tagsSection","type":"string","desc":"标签信息","required":false},{"name":"geographyContext","type":"string","desc":"已生成的地理环境XML数据","required":true},{"name":"powerSystemContext","type":"string","desc":"已生成的力量体系XML数据","required":true}]
```

**Template 4: llm_worldview_create UPDATE (PROMPT-04)**

UPDATE the existing unified template's active version content. Per D-06 and D-07, the updated content must:

1. Keep the same role definition and variable injection block
2. Keep only these 4 XML fields inside `<w>`:
   - `<t>` - 世界类型
   - `<b>` - 世界背景 (CDATA)
   - `<l>` - 时间线 (CDATA)
   - `<r>` - 世界规则 (CDATA)
3. REMOVE all `<g>`, `<p>`, `<f>` sections and their format explanations
4. REMOVE all geography/power-system/faction-specific generation requirements
5. Keep the general format rules (CDATA, no markdown, no explanation) adapted for the remaining fields
6. Keep the content quality requirements adapted for the 4 remaining fields

The UPDATE SQL pattern (following V3 approach but with corrected column name):
```sql
UPDATE ai_prompt_template_version
SET template_content = '...updated content...',
    version_comment = '精简世界观模板，移除地理环境/力量体系/阵营势力生成指令（已拆分为独立模板）'
WHERE template_id = (SELECT id FROM ai_prompt_template WHERE template_code = 'llm_worldview_create')
  AND is_active = 1;
```

IMPORTANT: Use WHERE template_code = (not WHERE code = as in V3, which was incorrect).

The updated template content should be structured as:
```
你是一位资深的网文世界观架构师...

【项目描述】
{projectDescription}

【故事类型】{storyGenre}
【故事基调】{storyTone}
{tagsSection}

根据以上项目描述，为这部小说创建世界观基础设定（地理环境、力量体系、阵营势力将在其他任务中独立生成）。

请严格按照以下XML格式返回：
<w>
  <t>世界类型</t>
  <b><![CDATA[世界背景描述（200-300字）]]></b>
  <l><![CDATA[时间线设定]]></l>
  <r><![CDATA[世界的基本规则和限制]]></r>
</w>

[format rules - CDATA, no markdown, no explanation]

[content requirements - adapted for remaining fields only]
```

After appending all SQL, the file should end with no trailing content.
  </action>
  <verify>
    <automated>cd ai-factory-backend && cat src/main/resources/db/migration/V4__independent_prompt_templates.sql | grep -c "llm_faction_create" && cat src/main/resources/db/migration/V4__independent_prompt_templates.sql | grep -c "template_code = 'llm_worldview_create'" && cat src/main/resources/db/migration/V4__independent_prompt_templates.sql | grep -c "geographyContext" && cat src/main/resources/db/migration/V4__independent_prompt_templates.sql | grep -c "powerSystemContext"</automated>
  </verify>
  <done>
    V4 migration file appended with:
    - INSERT for llm_faction_create master record (template_code='llm_faction_create', template_name='阵营势力独立生成')
    - INSERT for llm_faction_create version record containing {geographyContext} and {powerSystemContext} placeholders and <f> format
    - UPDATE setting current_version_id for llm_faction_create
    - UPDATE for llm_worldview_create template_content removing <g>/<p>/<f> sections, keeping only <t>/<b>/<l>/<r>
    - version_comment = '精简世界观模板，移除地理环境/力量体系/阵营势力生成指令（已拆分为独立模板）'
    - No usage of `WHERE code =` anywhere in the file (must use `WHERE template_code =`)
    - Total: 3 new template INSERTs (master + version + update-current-version-id each) + 1 existing template UPDATE
  </done>
</task>

</tasks>

<verification>
1. V4 migration file exists at ai-factory-backend/src/main/resources/db/migration/V4__independent_prompt_templates.sql
2. File contains exactly 3 INSERT INTO ai_prompt_template statements (geography, power_system, faction)
3. File contains exactly 3 INSERT INTO ai_prompt_template_version statements
4. File contains exactly 3 UPDATE ai_prompt_template statements for current_version_id
5. File contains exactly 1 UPDATE ai_prompt_template_version statement for llm_worldview_create
6. All WHERE clauses use `template_code` column (not `code`)
7. Geography template content uses `<r><n>名称</n><d>描述</d></r>` format (not `name="..."` attribute)
8. Faction template content contains `{geographyContext}` and `{powerSystemContext}` placeholders
9. Updated worldview template content does NOT contain `<g>`, `<p>`, or `<f>` format instructions
10. All variable placeholders use `{variableName}` format (Hutool StrUtil.format compatible)
</verification>

<success_criteria>
- V4 migration SQL file is syntactically valid and follows Flyway naming convention
- 3 new templates created: llm_geography_create, llm_power_system_create, llm_faction_create
- Each new template has a master record + version v1 record + current_version_id link
- Faction template has 6 variable placeholders including geographyContext and powerSystemContext
- Unified template updated to only output <t>/<b>/<l>/<r> fields
- No usage of incorrect column name `code` (always `template_code`)
- Geography template uses new <n>/<d> sub-tag format exclusively
</success_criteria>

<output>
After completion, create `.planning/phases/06-独立提示词模板/06-01-SUMMARY.md`
</output>
